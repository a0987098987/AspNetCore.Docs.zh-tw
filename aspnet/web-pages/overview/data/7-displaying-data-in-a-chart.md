---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: 資料顯示在圖表中的 ASP.NET Web Pages (Razor) |Microsoft Docs
author: microsoft
description: 本章說明如何在圖表中顯示資料。 在先前章節中，您已了解如何以手動方式和在方格中顯示資料。 本章說明...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 00529355476e88c47ab790121ae77202aa5e7b76
ms.sourcegitcommit: 45ac74e400f9f2b7dbded66297730f6f14a4eb25
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 08/16/2018
ms.locfileid: "41823598"
---
<a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="4583d-105">在與 ASP.NET Web Pages (Razor) 圖表中顯示資料</span><span class="sxs-lookup"><span data-stu-id="4583d-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>
====================
<span data-ttu-id="4583d-106">by [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="4583d-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="4583d-107">這篇文章說明如何使用圖表來顯示資料在 ASP.NET Web Pages (Razor) 網站中，使用`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="4583d-108">**您將學到什麼**:</span><span class="sxs-lookup"><span data-stu-id="4583d-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="4583d-109">如何在圖表中顯示資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="4583d-110">如何使用內建的佈景主題樣式圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="4583d-111">如何儲存圖表以及如何快取它們，以提升效能。</span><span class="sxs-lookup"><span data-stu-id="4583d-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="4583d-112">這些是 ASP.NET 程式設計文章中所引進的功能：</span><span class="sxs-lookup"><span data-stu-id="4583d-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="4583d-113">`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="4583d-114">這篇文章中的資訊適用於 ASP.NET Web Pages 1.0 和 Web Pages 2。</span><span class="sxs-lookup"><span data-stu-id="4583d-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>


<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="4583d-115">圖表協助程式</span><span class="sxs-lookup"><span data-stu-id="4583d-115">The Chart Helper</span></span>

<span data-ttu-id="4583d-116">當您想要的資料顯示在圖形表單中時，您可以使用`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="4583d-117">`Chart`協助程式可以呈現影像顯示各種不同的圖表類型的資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="4583d-118">它支援許多選項，用於格式化及標示。</span><span class="sxs-lookup"><span data-stu-id="4583d-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="4583d-119">`Chart`協助程式可以轉譯超過 30 種圖表，包括所有類型的圖表，您可能熟悉的 Microsoft Excel 或其他工具&#8212;區域圖、 橫條圖、 直條圖、 折線圖和圓餅圖，以及更多特製化的圖表，例如股票圖。</span><span class="sxs-lookup"><span data-stu-id="4583d-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="4583d-120">**區域圖**![描述： 區域圖類型的圖片](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="4583d-121">**橫條圖**![描述： 橫條圖類型的圖片](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="4583d-122">**直條圖**![描述： 直條圖類型的圖片](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="4583d-123">**折線圖**![描述： 折線圖類型的圖片](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="4583d-124">**圓形圖**![描述： 圓形圖類型的圖片](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="4583d-125">**股票圖**![描述： 股票圖類型的圖片](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="4583d-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="4583d-126">圖表項目</span><span class="sxs-lookup"><span data-stu-id="4583d-126">Chart Elements</span></span>

<span data-ttu-id="4583d-127">圖表會顯示資料和其他元素，例如圖例、 軸、 數列和等等。</span><span class="sxs-lookup"><span data-stu-id="4583d-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="4583d-128">下圖顯示的圖表元素，當您使用時，您可以自訂許多`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="4583d-129">本文說明如何設定一些 （並非全部） 這些項目。</span><span class="sxs-lookup"><span data-stu-id="4583d-129">This article shows you how to set some (not all) of these elements.</span></span>

![描述： 圖片顯示圖表項目](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="4583d-131">從資料建立圖表</span><span class="sxs-lookup"><span data-stu-id="4583d-131">Creating a Chart from Data</span></span>

<span data-ttu-id="4583d-132">您在圖表中顯示的資料可以是陣列，從結果中傳回，從資料庫或 XML 檔案中的資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="4583d-133">使用陣列</span><span class="sxs-lookup"><span data-stu-id="4583d-133">Using an Array</span></span>

<span data-ttu-id="4583d-134">中所述[ASP.NET Web Pages 程式設計使用 Razor 語法簡介](https://go.microsoft.com/fwlink/?LinkId=202890)，陣列可讓您將一組類似的項目儲存在單一變數。</span><span class="sxs-lookup"><span data-stu-id="4583d-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="4583d-135">您可以使用陣列來包含您想要併入圖表中的資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="4583d-136">此程序示範如何建立圖表資料在陣列中，從使用預設的圖表類型。</span><span class="sxs-lookup"><span data-stu-id="4583d-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="4583d-137">它也會示範如何顯示在頁面中的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="4583d-138">建立新的檔案，名為*ChartArrayBasic.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="4583d-139">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="4583d-140">程式碼第一次建立新的圖表，並設定其寬度和高度。</span><span class="sxs-lookup"><span data-stu-id="4583d-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="4583d-141">使用指定圖表標題`AddTitle`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="4583d-142">若要新增的資料，您使用`AddSeries`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="4583d-143">在此範例中，您可以使用`name`， `xValue`，並`yValues`參數`AddSeries`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="4583d-144">`name`參數會顯示在圖表圖例。</span><span class="sxs-lookup"><span data-stu-id="4583d-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="4583d-145">`xValue`參數包含圖表的水平軸顯示之資料的陣列。</span><span class="sxs-lookup"><span data-stu-id="4583d-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="4583d-146">`yValues`參數會包含用來繪製圖表垂直點的資料陣列。</span><span class="sxs-lookup"><span data-stu-id="4583d-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="4583d-147">`Write`方法實際呈現的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="4583d-148">在此情況下，因為您未指定圖表類型， `Chart` helper 會呈現其預設的圖表，也就是直條圖。</span><span class="sxs-lookup"><span data-stu-id="4583d-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="4583d-149">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-149">Run the page in the browser.</span></span> <span data-ttu-id="4583d-150">瀏覽器會顯示該圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="4583d-151">使用圖表資料的資料庫查詢</span><span class="sxs-lookup"><span data-stu-id="4583d-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="4583d-152">如果您想要繪製的資訊是在資料庫中，您可以執行資料庫查詢，然後使用結果的資料建立圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="4583d-153">此程序會示範如何讀取和顯示文件中所建立的資料庫中的資料[使用 ASP.NET Web Pages 網站中的資料庫簡介](https://go.microsoft.com/fwlink/?LinkId=202893)。</span><span class="sxs-lookup"><span data-stu-id="4583d-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="4583d-154">新增*應用程式\_資料*如果資料夾不存在的網站根目錄的資料夾。</span><span class="sxs-lookup"><span data-stu-id="4583d-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="4583d-155">在*應用程式\_資料*資料夾中，加入名為資料庫檔案*SmallBakery.sdf*中所述的[簡介使用 ASP.NET Web Pages 站台中中的資料庫](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="4583d-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="4583d-156">建立新的檔案，名為*ChartDataQuery.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="4583d-157">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="4583d-158">程式碼首先會開啟 SmallBakery 資料庫，並將它指派給變數，名為`db`。</span><span class="sxs-lookup"><span data-stu-id="4583d-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="4583d-159">此變數代表`Database`可用來讀取和寫入至資料庫的物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="4583d-160">接下來，程式碼會執行 SQL 查詢，以取得每個產品價格與名稱。</span><span class="sxs-lookup"><span data-stu-id="4583d-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="4583d-161">程式碼會建立新的圖表，並傳遞給它的資料庫查詢，藉由呼叫圖表的`DataBindTable`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="4583d-162">這個方法會採用兩個參數：`dataSource`參數是用來查詢資料和`xField`參數可讓您設定資料行用於圖表的 x 軸。</span><span class="sxs-lookup"><span data-stu-id="4583d-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="4583d-163">若要使用替代`DataBindTable`方法，您可以使用`AddSeries`方法`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="4583d-164">`AddSeries`方法可讓您設定`xValue`和`yValues`參數。</span><span class="sxs-lookup"><span data-stu-id="4583d-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="4583d-165">比方說，而不是使用`DataBindTable`方法如下：</span><span class="sxs-lookup"><span data-stu-id="4583d-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="4583d-166">您可以使用`AddSeries`方法如下：</span><span class="sxs-lookup"><span data-stu-id="4583d-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="4583d-167">轉譯相同的結果。</span><span class="sxs-lookup"><span data-stu-id="4583d-167">Both render the same results.</span></span> <span data-ttu-id="4583d-168">`AddSeries`方法會更有彈性，因為您可以更明確地指定的圖表類型和資料但`DataBindTable`方法較為容易使用，如果您不需要額外的彈性。</span><span class="sxs-lookup"><span data-stu-id="4583d-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="4583d-169">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="4583d-170">使用 XML 資料</span><span class="sxs-lookup"><span data-stu-id="4583d-170">Using XML Data</span></span>

<span data-ttu-id="4583d-171">圖表的第三個選項是使用圖表做為資料的 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="4583d-172">這需要的 XML 檔案也具有結構描述檔案 (*.xsd*檔案) 的 XML 結構描述。</span><span class="sxs-lookup"><span data-stu-id="4583d-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="4583d-173">此程序會示範如何從 XML 檔案讀取資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="4583d-174">在 *應用程式\_資料*資料夾中，建立新的 XML 檔案，名為*data.xml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="4583d-175">下列程式碼，也就是一家虛構公司員工相關的某些 XML 資料取代現有的 XML。</span><span class="sxs-lookup"><span data-stu-id="4583d-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="4583d-176">在 *應用程式\_資料*資料夾中，建立新的 XML 檔案，名為*data.xsd*。</span><span class="sxs-lookup"><span data-stu-id="4583d-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="4583d-177">(請注意，擴充功能目前是 *.xsd*。)</span><span class="sxs-lookup"><span data-stu-id="4583d-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="4583d-178">以下列內容取代現有的 XML:</span><span class="sxs-lookup"><span data-stu-id="4583d-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="4583d-179">在網站根目錄中，建立新的檔案，名為*ChartDataXML.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="4583d-180">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="4583d-181">程式碼會先建立`DataSet`物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="4583d-182">此物件用來管理的資料會從 XML 檔案讀取，並組織根據結構描述檔案中的資訊。</span><span class="sxs-lookup"><span data-stu-id="4583d-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="4583d-183">(請注意頂端的程式碼包含陳述式`using SystemData`。</span><span class="sxs-lookup"><span data-stu-id="4583d-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="4583d-184">這為了能夠使用必要`DataSet`物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="4583d-185">如需詳細資訊，請參閱 < [ &quot;Using&quot;陳述式和完整名稱](#SB_UsingStatements)本文稍後。)</span><span class="sxs-lookup"><span data-stu-id="4583d-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="4583d-186">接下來，程式碼會建立`DataView`資料集為基礎的物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="4583d-187">資料檢視會提供圖表可以繫結至物件&#8212;也就是讀取，並繪製。</span><span class="sxs-lookup"><span data-stu-id="4583d-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="4583d-188">圖表繫結至資料`AddSeries`方法，為您稍早時看到這次圖表陣列資料，不同之處在於`xValue`並`yValues`參數會設定為`DataView`物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="4583d-189">此範例也示範如何指定特定圖表類型。</span><span class="sxs-lookup"><span data-stu-id="4583d-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="4583d-190">在 新增資料時`AddSeries`方法，`chartType`參數也會設定為顯示圓形圖。</span><span class="sxs-lookup"><span data-stu-id="4583d-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="4583d-191">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="4583d-192">"Using"陳述式和完整格式的名稱</span><span class="sxs-lookup"><span data-stu-id="4583d-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="4583d-193">含有 Razor 語法的 ASP.NET Web Pages 根據.NET Framework 是由數千個元件 （類別） 所組成。</span><span class="sxs-lookup"><span data-stu-id="4583d-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="4583d-194">若要使用所有這些類別可管理，它們會組織成*命名空間*，這是有點像程式庫。</span><span class="sxs-lookup"><span data-stu-id="4583d-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="4583d-195">例如，`System.Web`命名空間包含類別，可支援瀏覽器/伺服器通訊`System.Xml`命名空間包含類別，用來建立和讀取 XML 檔案，和`System.Data`命名空間包含類別，可讓您處理使用資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="4583d-196">若要存取.NET Framework 中的任何特定的類別，程式碼需要知道不只是類別名稱，但也表示類別仍在命名空間。</span><span class="sxs-lookup"><span data-stu-id="4583d-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="4583d-197">例如，若要使用`Chart`協助程式，必須先找到的程式碼`System.Web.Helpers.Chart`類別，其結合了命名空間 (`System.Web.Helpers`) 類別名稱 (`Chart`)。</span><span class="sxs-lookup"><span data-stu-id="4583d-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="4583d-198">這就所謂的類別*完整*名稱&#8212;其完整的模稜兩可的位置內 vastness 的.NET framework。</span><span class="sxs-lookup"><span data-stu-id="4583d-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="4583d-199">在程式碼，這看起來如下所示：</span><span class="sxs-lookup"><span data-stu-id="4583d-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="4583d-200">不過，很麻煩 （又容易出錯） 能夠使用這些長、 完整的名稱，每次您想要參考類別或協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="4583d-201">因此，若要讓您更輕鬆地使用類別名稱，您可以*匯入*的命名空間您感興趣，這通常是您只在從.NET Framework 中的許多命名空間中。</span><span class="sxs-lookup"><span data-stu-id="4583d-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="4583d-202">如果您已匯入命名空間，您可以使用的類別名稱 (`Chart`) 而不是完整限定名稱 (`System.Web.Helpers.Chart`)。</span><span class="sxs-lookup"><span data-stu-id="4583d-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="4583d-203">當您的程式碼執行，且遇到類別名稱時，它看起來可能只是您已匯入到該類別的命名空間中。</span><span class="sxs-lookup"><span data-stu-id="4583d-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="4583d-204">當您使用含有 Razor 語法的 ASP.NET Web Pages 建立 web 網頁上時，您通常使用相同的類別集，每次包括`WebPage`類別、 各種不同的協助程式等等。</span><span class="sxs-lookup"><span data-stu-id="4583d-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="4583d-205">若要儲存匯入相關的命名空間，每次您建立網站的工作，因此它會自動匯入一組核心命名空間，每個網站設定 ASP.NET。</span><span class="sxs-lookup"><span data-stu-id="4583d-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="4583d-206">這就是為什麼您還沒有必須處理命名空間，或匯入為止;您曾經使用過的所有類別都都會在已為您匯入的命名空間中。</span><span class="sxs-lookup"><span data-stu-id="4583d-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="4583d-207">不過，有時候您會需要使用不會自動為您匯入的命名空間中的類別。</span><span class="sxs-lookup"><span data-stu-id="4583d-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="4583d-208">在此情況下，您可以使用該類別的完整名稱，或您手動匯入的命名空間包含的類別。</span><span class="sxs-lookup"><span data-stu-id="4583d-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="4583d-209">若要匯入命名空間，您使用`using`陳述式 (`import` Visual Basic 中)，如您稍早在範例中看到一文。</span><span class="sxs-lookup"><span data-stu-id="4583d-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="4583d-210">例如，`DataSet`類別位於`System.Data`命名空間。</span><span class="sxs-lookup"><span data-stu-id="4583d-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="4583d-211">`System.Data`命名空間不會自動提供給 ASP.NET Razor 頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="4583d-212">因此，若要使用`DataSet`類別使用其完整的名稱，您可以使用如下的程式碼：</span><span class="sxs-lookup"><span data-stu-id="4583d-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="4583d-213">如果您必須使用`DataSet`類別重複您可以匯入像這樣的命名空間，然後使用程式碼中的 類別名稱：</span><span class="sxs-lookup"><span data-stu-id="4583d-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="4583d-214">您可以新增`using`陳述式，針對您想要參考的任何其他.NET Framework 命名空間。</span><span class="sxs-lookup"><span data-stu-id="4583d-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="4583d-215">不過，如前所述，您不需要這種做法，因為大部分的類別，您將使用的是會自動匯入 asp.net 中使用的命名空間中 *.cshtml*並 *.vbhtml*頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>


<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="4583d-216">顯示在 Web 網頁內的圖表</span><span class="sxs-lookup"><span data-stu-id="4583d-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="4583d-217">在範例中您已經看到截至目前為止，您建立的圖表，則圖表會呈現直接在瀏覽器為圖形。</span><span class="sxs-lookup"><span data-stu-id="4583d-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="4583d-218">在許多情況下，不過，您想要顯示圖表 頁面上，不只是依本身在瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="4583d-219">若要這樣做需要雙步驟程序。</span><span class="sxs-lookup"><span data-stu-id="4583d-219">To do that requires a two-step process.</span></span> <span data-ttu-id="4583d-220">第一個步驟是建立會產生圖表，頁面，如您所見。</span><span class="sxs-lookup"><span data-stu-id="4583d-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="4583d-221">第二個步驟是在另一個網頁中顯示產生的映像。</span><span class="sxs-lookup"><span data-stu-id="4583d-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="4583d-222">若要顯示的映像，您可以使用 HTML`<img>`中相同的項目，您通常用來顯示任何映像的方式。</span><span class="sxs-lookup"><span data-stu-id="4583d-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="4583d-223">不過，而不是參考 *.jpg*或是 *.png*檔案中，`<img>`項目參考 *.cshtml*檔案，其中包含`Chart`協助程式，建立的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="4583d-224">顯示頁面時執行，則`<img>`項目取得的輸出`Chart`協助程式，並轉譯圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="4583d-225">建立名為*ShowChart.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="4583d-226">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="4583d-227">程式碼會使用`<img>`項目，以顯示您稍早建立的圖表*ChartArrayBasic.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="4583d-228">在瀏覽器中執行之 web 網頁。</span><span class="sxs-lookup"><span data-stu-id="4583d-228">Run the web page in a browser.</span></span> <span data-ttu-id="4583d-229">*ShowChart.cshtml*檔案會顯示包含在程式碼為基礎的圖表影像*ChartArrayBasic.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="4583d-230">設定圖表的樣式</span><span class="sxs-lookup"><span data-stu-id="4583d-230">Styling a Chart</span></span>

<span data-ttu-id="4583d-231">`Chart`協助程式支援大量的選項可讓您自訂圖表的外觀。</span><span class="sxs-lookup"><span data-stu-id="4583d-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="4583d-232">您可以設定色彩、 字型、 框線和等等。</span><span class="sxs-lookup"><span data-stu-id="4583d-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="4583d-233">自訂圖表的外觀的簡單方法是使用*佈景主題*。</span><span class="sxs-lookup"><span data-stu-id="4583d-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="4583d-234">佈景主題是資訊的指定方式來呈現圖表使用的字型、 色彩、 標籤、 調色盤、 框線和效果的集合。</span><span class="sxs-lookup"><span data-stu-id="4583d-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="4583d-235">（請注意圖表的樣式不會指出類型的圖表）。</span><span class="sxs-lookup"><span data-stu-id="4583d-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="4583d-236">下表列出內建的佈景主題。</span><span class="sxs-lookup"><span data-stu-id="4583d-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="4583d-237">主題</span><span class="sxs-lookup"><span data-stu-id="4583d-237">Theme</span></span> | <span data-ttu-id="4583d-238">描述</span><span class="sxs-lookup"><span data-stu-id="4583d-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="4583d-239">白色背景上顯示紅色的資料行。</span><span class="sxs-lookup"><span data-stu-id="4583d-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="4583d-240">顯示藍色上藍色漸層背景的資料行。</span><span class="sxs-lookup"><span data-stu-id="4583d-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="4583d-241">顯示藍色上綠色漸層背景的資料行。</span><span class="sxs-lookup"><span data-stu-id="4583d-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="4583d-242">黃色漸層背景上顯示橘色的資料行。</span><span class="sxs-lookup"><span data-stu-id="4583d-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="4583d-243">白色背景上顯示 3d 紅色的資料行。</span><span class="sxs-lookup"><span data-stu-id="4583d-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="4583d-244">您可以指定要建立新的圖表時所使用之佈景主題。</span><span class="sxs-lookup"><span data-stu-id="4583d-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="4583d-245">建立新的檔案，名為*ChartStyleGreen.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="4583d-246">以下列內容取代現有的內容頁面中：</span><span class="sxs-lookup"><span data-stu-id="4583d-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="4583d-247">此程式碼等同於先前的範例，會使用資料庫的資料，但會增加`theme`參數時，它會建立`Chart`物件。</span><span class="sxs-lookup"><span data-stu-id="4583d-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="4583d-248">下列範例顯示已變更的程式碼：</span><span class="sxs-lookup"><span data-stu-id="4583d-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="4583d-249">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-249">Run the page in a browser.</span></span> <span data-ttu-id="4583d-250">您會看到與之前相同的資料，但圖表看起來更精良：</span><span class="sxs-lookup"><span data-stu-id="4583d-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="4583d-251">儲存圖表</span><span class="sxs-lookup"><span data-stu-id="4583d-251">Saving a Chart</span></span>

<span data-ttu-id="4583d-252">當您使用`Chart`為您的協助程式為止所看到本文章會協助程式會重新建立從頭圖表每次叫用時。</span><span class="sxs-lookup"><span data-stu-id="4583d-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="4583d-253">如有必要，圖表的程式碼也會重新查詢資料庫，或重新讀取 XML 檔案，以取得資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="4583d-254">在某些情況下，執行此動作可以是複雜的作業，例如您想要查詢的資料庫很大，則 XML 檔案包含大量資料。</span><span class="sxs-lookup"><span data-stu-id="4583d-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="4583d-255">即使圖表不需要用到大量資料，以動態方式建立映像的程序佔用伺服器資源，以及如果許多人要求的頁面或頁面會顯示圖表，有可能會影響您的網站的效能。</span><span class="sxs-lookup"><span data-stu-id="4583d-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="4583d-256">為了協助您降低建立圖表的潛在效能影響，您可以建立圖表第一次您需要它，然後儲存它。</span><span class="sxs-lookup"><span data-stu-id="4583d-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="4583d-257">當圖表需要一次，而不是重新產生它，您也可以擷取已儲存的版本，並呈現的。</span><span class="sxs-lookup"><span data-stu-id="4583d-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="4583d-258">您可以透過下列方式來儲存圖表：</span><span class="sxs-lookup"><span data-stu-id="4583d-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="4583d-259">快取 （伺服器） 上的電腦記憶體中的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="4583d-260">將圖表儲存為影像檔。</span><span class="sxs-lookup"><span data-stu-id="4583d-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="4583d-261">將圖表儲存為 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-261">Save the chart as an XML file.</span></span> <span data-ttu-id="4583d-262">此選項可讓您修改圖表，再加以儲存。</span><span class="sxs-lookup"><span data-stu-id="4583d-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="4583d-263">快取的圖表</span><span class="sxs-lookup"><span data-stu-id="4583d-263">Caching a Chart</span></span>

<span data-ttu-id="4583d-264">建立圖表之後，您就可以將它快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="4583d-265">快取的圖表，以表示它不一定會再次顯示，需要要重新建立。</span><span class="sxs-lookup"><span data-stu-id="4583d-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="4583d-266">當您將圖表儲存在快取時，您指定對該圖表必須是唯一的索引鍵。</span><span class="sxs-lookup"><span data-stu-id="4583d-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="4583d-267">如果在伺服器記憶體不足，可能會移除儲存至快取的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="4583d-268">此外，如果因為任何原因而重新啟動您的應用程式，會清除快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="4583d-269">因此，標準的方式來使用快取的圖表是一律先檢查其是否可快取中，和如果沒有，則建立或重建它。</span><span class="sxs-lookup"><span data-stu-id="4583d-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="4583d-270">在您的網站根目錄中，建立名為*ShowCachedChart.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="4583d-271">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="4583d-272">`<img>`標記會包括`src`屬性，指向*ChartSaveToCache.cshtml*檔案，並將金鑰傳遞至查詢字串的頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="4583d-273">索引鍵包含值&quot;myChartKey&quot;。</span><span class="sxs-lookup"><span data-stu-id="4583d-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="4583d-274">*ChartSaveToCache.cshtml*檔案包含`Chart`協助程式建立的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="4583d-275">稍後，您將建立此頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="4583d-276">結尾的頁面中，沒有名為頁面的連結*ClearCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="4583d-277">這是您也會短時間內建立的頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="4583d-278">您需要*ClearCache.cshtml*來測試此範例中的快取，它不是連結或使用快取的圖表時，通常會包含的頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="4583d-279">在您的網站根目錄中，建立新的檔案，名為*ChartSaveToCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="4583d-280">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="4583d-281">程式碼會先檢查是否任何項目傳遞作為查詢字串中的索引鍵值。</span><span class="sxs-lookup"><span data-stu-id="4583d-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="4583d-282">因此，此程式碼嘗試讀取從快取的圖表，藉由呼叫如果`GetFromCache`方法，並將金鑰傳遞給它。</span><span class="sxs-lookup"><span data-stu-id="4583d-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="4583d-283">事實上，沒有任何快取下該索引鍵 （這會發生第一次要求圖表） 中，如果程式碼會如往常般建立圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="4583d-284">圖表完成後，程式碼將它儲存至快取藉由呼叫`SaveToCache`。</span><span class="sxs-lookup"><span data-stu-id="4583d-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="4583d-285">該方法需要索引鍵，因此可以在稍後要求圖表） 和圖表應該儲存在快取的時間量。</span><span class="sxs-lookup"><span data-stu-id="4583d-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="4583d-286">（您會快取圖表的確切時間會取決於您認為它所代表的資料可能會變更的頻率）。`SaveToCache`方法也需要`slidingExpiration`參數&#8212;如果此值設為 true，在逾時計數器會重設每次存取時的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="4583d-287">在此情況下，它實際上表示圖表的快取項目到期之後有人存取圖表的最後一個時間的 2 分鐘。</span><span class="sxs-lookup"><span data-stu-id="4583d-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="4583d-288">（滑動期限的替代方案是絕對期限，這表示快取項目會到期剛好 2 分鐘後已把它放入快取中，不論它有已存取頻率）。</span><span class="sxs-lookup"><span data-stu-id="4583d-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="4583d-289">最後，程式碼會使用`WriteFromCache`方法來擷取和轉譯來自快取的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="4583d-290">請注意，這個方法是外部`if`檢查快取，因為它會從快取取得圖表，圖表是否有一開始，或必須產生並儲存在快取的區塊。</span><span class="sxs-lookup"><span data-stu-id="4583d-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="4583d-291">請注意，在此範例中，`AddTitle`方法包含時間戳記。</span><span class="sxs-lookup"><span data-stu-id="4583d-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="4583d-292">(目前的日期和時間，它會新增&#8212; `DateTime.Now` &#8212;標題。)</span><span class="sxs-lookup"><span data-stu-id="4583d-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="4583d-293">建立名為的新頁面*ClearCache.cshtml*和其內容取代為下列：</span><span class="sxs-lookup"><span data-stu-id="4583d-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="4583d-294">此頁面會使用`WebCache`若要移除圖表中快取的 helper *ChartSaveToCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="4583d-295">如先前所述，您通常不必像這樣的頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="4583d-296">您要建立這裡只是為了讓您更輕鬆地測試快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="4583d-297">執行*ShowCachedChart.cshtml*網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="4583d-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="4583d-298">此頁面會顯示圖表影像中所包含的程式碼為基礎*ChartSaveToCache.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="4583d-299">記下的圖表標題中顯示的時間戳記。</span><span class="sxs-lookup"><span data-stu-id="4583d-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![圖表標題中具有時間戳記的基本圖描述： 圖片](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="4583d-301">關閉瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="4583d-301">Close the browser.</span></span>
8. <span data-ttu-id="4583d-302">執行*ShowCachedChart.cshtml*一次。</span><span class="sxs-lookup"><span data-stu-id="4583d-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="4583d-303">請注意，時間戳記相同，表示圖表未重新產生，但改為從快取中讀取。</span><span class="sxs-lookup"><span data-stu-id="4583d-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="4583d-304">在  *ShowCachedChart.cshtml*，按一下**清除快取**連結。</span><span class="sxs-lookup"><span data-stu-id="4583d-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="4583d-305">這會帶您前往*ClearCache.cshtml*，這會報告已清除快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="4583d-306">按一下 **返回 ShowCachedChart.cshtml**連結，或重新執行*ShowCachedChart.cshtml*從 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="4583d-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="4583d-307">請注意，這次的時間戳記已變更，因為在清除快取也一樣。</span><span class="sxs-lookup"><span data-stu-id="4583d-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="4583d-308">因此，程式碼，就必須重新產生圖表，並把它放回快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="4583d-309">將圖表儲存為影像檔</span><span class="sxs-lookup"><span data-stu-id="4583d-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="4583d-310">您也可以儲存圖表為影像檔 (例如 *.jpg*檔案) 的伺服器上。</span><span class="sxs-lookup"><span data-stu-id="4583d-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="4583d-311">任何映像的方式時，可以使用映像檔。</span><span class="sxs-lookup"><span data-stu-id="4583d-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="4583d-312">優點是，檔案會儲存，而不是儲存至暫時快取。</span><span class="sxs-lookup"><span data-stu-id="4583d-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="4583d-313">您可以將新的圖表影像儲存在不同的時間 （例如，每隔一小時），並再記錄永久的一段時間的變更。</span><span class="sxs-lookup"><span data-stu-id="4583d-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="4583d-314">請注意，您必須先確定您的 web 應用程式已將檔案儲存至資料夾中，您要將映像檔案放在伺服器上的權限。</span><span class="sxs-lookup"><span data-stu-id="4583d-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="4583d-315">在您的網站根目錄中，建立名為 *\_ChartFiles*如果不存在。</span><span class="sxs-lookup"><span data-stu-id="4583d-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="4583d-316">在您的網站根目錄中，建立新的檔案，名為*ChartSave.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="4583d-317">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="4583d-318">程式碼會先檢查以查看是否 *.jpg*檔案存在，藉由呼叫`File.Exists`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="4583d-319">如果檔案不存在，程式碼會建立新`Chart`從陣列。</span><span class="sxs-lookup"><span data-stu-id="4583d-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="4583d-320">此時，程式碼會呼叫`Save`方法，並傳遞`path`參數指定的檔案路徑和檔案名稱儲存圖表的位置。</span><span class="sxs-lookup"><span data-stu-id="4583d-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="4583d-321">在頁面中，主體`<img>`項目會使用路徑來指向 *.jpg*檔案來顯示。</span><span class="sxs-lookup"><span data-stu-id="4583d-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="4583d-322">執行*ChartSave.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="4583d-323">傳回至 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="4583d-323">Return to WebMatrix.</span></span> <span data-ttu-id="4583d-324">請注意，影像檔名為*chart01.jpg*就已經儲存在 *\_ChartFiles*資料夾。</span><span class="sxs-lookup"><span data-stu-id="4583d-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="4583d-325">將圖表儲存為 XML 檔案</span><span class="sxs-lookup"><span data-stu-id="4583d-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="4583d-326">最後，您可以將圖表儲存為 XML 檔案在伺服器上。</span><span class="sxs-lookup"><span data-stu-id="4583d-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="4583d-327">透過快取圖表或將圖表儲存至檔案中使用此方法的優點是您無法再顯示圖表，如果您想要修改的 XML。</span><span class="sxs-lookup"><span data-stu-id="4583d-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="4583d-328">您的應用程式必須要有您要將映像檔案放在伺服器上資料夾的讀取/寫入權限。</span><span class="sxs-lookup"><span data-stu-id="4583d-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="4583d-329">在您的網站根目錄中，建立新的檔案，名為*ChartSaveXml.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="4583d-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="4583d-330">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="4583d-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="4583d-331">此程式碼很類似您稍早看到將圖表儲存在快取中，不同之處在於它會使用 XML 檔案的程式碼。</span><span class="sxs-lookup"><span data-stu-id="4583d-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="4583d-332">程式碼會先檢查 XML 檔案是否存在藉由呼叫`File.Exists`方法。</span><span class="sxs-lookup"><span data-stu-id="4583d-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="4583d-333">如果檔案不存在，程式碼會建立新`Chart`物件，並傳遞的檔案名稱為`themePath`參數。</span><span class="sxs-lookup"><span data-stu-id="4583d-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="4583d-334">這會建立基礎 XML 檔案中的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="4583d-335">如果 XML 檔案不存在，程式碼會建立圖表，以正常的方式，並接著呼叫`SaveXml`將它儲存。</span><span class="sxs-lookup"><span data-stu-id="4583d-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="4583d-336">圖表會呈現使用`Write`方法，為您曾看過。</span><span class="sxs-lookup"><span data-stu-id="4583d-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="4583d-337">如同顯示快取的頁面上，此程式碼會在圖表標題中包含時間戳記。</span><span class="sxs-lookup"><span data-stu-id="4583d-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="4583d-338">建立名為的新頁面*ChartDisplayXMLChart.cshtml*並加入下列標記：</span><span class="sxs-lookup"><span data-stu-id="4583d-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="4583d-339">執行*ChartDisplayXMLChart.cshtml*頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="4583d-340">圖表會顯示。</span><span class="sxs-lookup"><span data-stu-id="4583d-340">The chart is displayed.</span></span> <span data-ttu-id="4583d-341">記下的時間戳記在圖表的標題。</span><span class="sxs-lookup"><span data-stu-id="4583d-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="4583d-342">關閉瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="4583d-342">Close the browser.</span></span>
6. <span data-ttu-id="4583d-343">在 WebMatrix 中，以滑鼠右鍵按一下 *\_ChartFiles*資料夾中，按一下 **重新整理**，然後再開啟資料夾。</span><span class="sxs-lookup"><span data-stu-id="4583d-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="4583d-344">*XMLChart.xml*此資料夾中的檔案由`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="4583d-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![描述： _ChartFiles 資料夾顯示圖表協助程式 」 所建立的 XMLChart.xml 檔案。](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="4583d-346">執行*ChartDisplayXMLChart.cshtml*頁面上一次。</span><span class="sxs-lookup"><span data-stu-id="4583d-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="4583d-347">圖表會顯示相同的時間戳記，當做第一次執行的頁面。</span><span class="sxs-lookup"><span data-stu-id="4583d-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="4583d-348">這是因為從您稍早儲存的 XML 產生的圖表。</span><span class="sxs-lookup"><span data-stu-id="4583d-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="4583d-349">在 WebMatrix 中開啟 *\_ChartFiles*資料夾，然後刪除*XMLChart.xml*檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="4583d-350">執行*ChartDisplayXMLChart.cshtml*頁面上一次。</span><span class="sxs-lookup"><span data-stu-id="4583d-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="4583d-351">這次更新時間戳記，因為`Chart`helper 必須重新建立 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="4583d-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="4583d-352">如果您想，檢查 *\_ChartFiles*資料夾，請注意，XML 檔案已恢復。</span><span class="sxs-lookup"><span data-stu-id="4583d-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="4583d-353">其他資源</span><span class="sxs-lookup"><span data-stu-id="4583d-353">Additional Resources</span></span>

- [<span data-ttu-id="4583d-354">簡介使用的資料庫，在 ASP.NET Web Pages 網站</span><span class="sxs-lookup"><span data-stu-id="4583d-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="4583d-355">使用快取的 ASP.NET Web Pages 站台，以改善效能</span><span class="sxs-lookup"><span data-stu-id="4583d-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="4583d-356">[圖表類別](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99))（MSDN 上的 ASP.NET 網頁 API 參考）</span><span class="sxs-lookup"><span data-stu-id="4583d-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
