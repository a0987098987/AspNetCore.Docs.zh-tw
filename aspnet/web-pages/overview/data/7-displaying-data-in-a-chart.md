---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: 顯示資料在圖表中以 ASP.NET Web Pages (Razor) |Microsoft 文件
author: microsoft
description: 本章節將說明如何在圖表中顯示資料。 在先前章節中，您將學會如何以手動方式，在方格中顯示資料。 本章說明...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/22/2012
ms.topic: article
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
ms.technology: dotnet-webpages
ms.prod: .net-framework
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: 5cf17e54408d585e9a375b302b61b4e28d9b022a
ms.sourcegitcommit: f8852267f463b62d7f975e56bea9aa3f68fbbdeb
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 04/06/2018
---
<a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="e411c-105">顯示資料在圖表中以 ASP.NET Web Pages (Razor)</span><span class="sxs-lookup"><span data-stu-id="e411c-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>
====================
<span data-ttu-id="e411c-106">by [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="e411c-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="e411c-107">這篇文章說明如何使用圖表來顯示資料在 ASP.NET Web Pages (Razor) 的網站中，使用`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="e411c-108">**您將學習**:</span><span class="sxs-lookup"><span data-stu-id="e411c-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="e411c-109">如何在圖表中顯示資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="e411c-110">如何使用內建的佈景主題樣式圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="e411c-111">如何儲存圖表以及如何快取它們，以提升效能。</span><span class="sxs-lookup"><span data-stu-id="e411c-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="e411c-112">這些是 ASP.NET 程式設計文件中所引進的功能：</span><span class="sxs-lookup"><span data-stu-id="e411c-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="e411c-113">`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="e411c-114">本主題資訊適用於 ASP.NET Web Pages 1.0 和 Web Pages 2。</span><span class="sxs-lookup"><span data-stu-id="e411c-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>


<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="e411c-115">圖表協助程式</span><span class="sxs-lookup"><span data-stu-id="e411c-115">The Chart Helper</span></span>

<span data-ttu-id="e411c-116">當您想要的資料顯示在圖形表單中時，您可以使用`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="e411c-117">`Chart` Helper 可以呈現影像中各種不同的圖表類型顯示資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="e411c-118">格式化及標示支援許多選項。</span><span class="sxs-lookup"><span data-stu-id="e411c-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="e411c-119">`Chart` Helper 可以轉譯 30 個以上類型的圖表，包括所有類型的圖表，您可能已經熟悉 Microsoft Excel 或其他工具中&#8212;區域圖、 橫條圖、 直條圖、 折線圖、 和圓餅圖，以及多個特殊的圖表，例如股票圖。</span><span class="sxs-lookup"><span data-stu-id="e411c-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="e411c-120">**區域圖**![描述： 區域圖類型的圖片](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="e411c-121">**橫條圖**![描述： 橫條圖類型的圖片](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="e411c-122">**直條圖**![描述： 直條圖類型的圖片](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="e411c-123">**折線圖**![描述： 折線圖類型的圖片](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="e411c-124">**圓形圖**![描述： 圓形圖類型的圖片](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="e411c-125">**股票圖**![描述： 股票圖類型的圖片](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="e411c-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="e411c-126">圖表項目</span><span class="sxs-lookup"><span data-stu-id="e411c-126">Chart Elements</span></span>

<span data-ttu-id="e411c-127">圖表會顯示資料和其他元素，例如圖例、 軸、 數列和等等。</span><span class="sxs-lookup"><span data-stu-id="e411c-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="e411c-128">下圖顯示的圖表項目，當您使用時，您可以自訂許多`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="e411c-129">本文將說明如何設定某些 （並非全部） 的這些項目。</span><span class="sxs-lookup"><span data-stu-id="e411c-129">This article shows you how to set some (not all) of these elements.</span></span>

![描述： 圖片顯示圖表項目](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="e411c-131">從資料建立圖表</span><span class="sxs-lookup"><span data-stu-id="e411c-131">Creating a Chart from Data</span></span>

<span data-ttu-id="e411c-132">您在圖表中顯示的資料可以是陣列，從資料庫中，從傳回的結果或 XML 檔案中的資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="e411c-133">使用陣列</span><span class="sxs-lookup"><span data-stu-id="e411c-133">Using an Array</span></span>

<span data-ttu-id="e411c-134">中所述[ASP.NET Web Pages 程式設計使用 Razor 語法的簡介](https://go.microsoft.com/fwlink/?LinkId=202890)，陣列可讓您在單一的變數中儲存的類似項目集合。</span><span class="sxs-lookup"><span data-stu-id="e411c-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="e411c-135">您可以使用陣列，包含您想要包含在圖表中的資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="e411c-136">此程序示範如何建立圖表資料在陣列中，從使用預設的圖表類型。</span><span class="sxs-lookup"><span data-stu-id="e411c-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="e411c-137">它也會示範如何顯示在頁面內的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="e411c-138">建立新的檔案命名為*ChartArrayBasic.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="e411c-139">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="e411c-140">程式碼會先建立新的圖表，並設定其寬度和高度。</span><span class="sxs-lookup"><span data-stu-id="e411c-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="e411c-141">使用指定的圖表標題`AddTitle`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="e411c-142">若要新增的資料，您使用`AddSeries`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="e411c-143">在此範例中，您會使用`name`， `xValue`，和`yValues`參數`AddSeries`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="e411c-144">`name`參數會顯示在圖表圖例中。</span><span class="sxs-lookup"><span data-stu-id="e411c-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="e411c-145">`xValue`參數會包含沿著圖表的水平軸顯示的資料陣列。</span><span class="sxs-lookup"><span data-stu-id="e411c-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="e411c-146">`yValues`參數包含用來繪製圖表垂直點的資料陣列。</span><span class="sxs-lookup"><span data-stu-id="e411c-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="e411c-147">`Write`方法實際上會呈現的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="e411c-148">在此情況下，因為您沒有指定圖表類型， `Chart` helper 呈現其預設的圖表，這是直條圖。</span><span class="sxs-lookup"><span data-stu-id="e411c-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="e411c-149">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-149">Run the page in the browser.</span></span> <span data-ttu-id="e411c-150">瀏覽器會顯示該圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="e411c-151">使用圖表資料的資料庫查詢</span><span class="sxs-lookup"><span data-stu-id="e411c-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="e411c-152">如果您想要繪製的資訊在資料庫中，您可以執行資料庫查詢，然後使用 建立該圖表的 結果的資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="e411c-153">此程序會示範如何讀取和顯示文件中建立的資料庫資料[簡介使用 ASP.NET Web Pages 站台中的資料庫](https://go.microsoft.com/fwlink/?LinkId=202893)。</span><span class="sxs-lookup"><span data-stu-id="e411c-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="e411c-154">新增*應用程式\_資料*如果資料夾不存在該網站的根資料夾。</span><span class="sxs-lookup"><span data-stu-id="e411c-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="e411c-155">在*應用程式\_資料*資料夾中，加入名為資料庫檔案*SmallBakery.sdf*所述[簡介使用 ASP.NET Web Pages 站台中中的資料庫](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="e411c-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="e411c-156">建立新的檔案命名為*ChartDataQuery.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="e411c-157">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="e411c-158">此程式碼第一次開啟 SmallBakery 資料庫，並將它指派給變數，名為`db`。</span><span class="sxs-lookup"><span data-stu-id="e411c-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="e411c-159">此變數代表`Database`可用來讀取和寫入至資料庫的物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="e411c-160">接下來，程式碼會執行 SQL 查詢，以取得每個產品價格與名稱。</span><span class="sxs-lookup"><span data-stu-id="e411c-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="e411c-161">程式碼建立新的圖表，並將資料庫查詢傳遞給它，藉由呼叫圖表的`DataBindTable`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="e411c-162">這個方法會採用兩個參數：`dataSource`參數是用來從查詢中，資料和`xField`參數可讓您設定資料行用於圖表的 x 軸。</span><span class="sxs-lookup"><span data-stu-id="e411c-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="e411c-163">做為使用替代`DataBindTable`方法，您可以使用`AddSeries`方法`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="e411c-164">`AddSeries`方法可讓您設定`xValue`和`yValues`參數。</span><span class="sxs-lookup"><span data-stu-id="e411c-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="e411c-165">例如，而不是使用`DataBindTable`方法如下：</span><span class="sxs-lookup"><span data-stu-id="e411c-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="e411c-166">您可以使用`AddSeries`方法如下：</span><span class="sxs-lookup"><span data-stu-id="e411c-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="e411c-167">兩者會呈現相同的結果。</span><span class="sxs-lookup"><span data-stu-id="e411c-167">Both render the same results.</span></span> <span data-ttu-id="e411c-168">`AddSeries`方法是更有彈性，因為您可以更明確地指定的圖表類型和資料，但`DataBindTable`方法比較容易使用，如果您不需要額外的彈性。</span><span class="sxs-lookup"><span data-stu-id="e411c-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="e411c-169">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="e411c-170">使用 XML 資料</span><span class="sxs-lookup"><span data-stu-id="e411c-170">Using XML Data</span></span>

<span data-ttu-id="e411c-171">圖表的第三個選項是使用 XML 檔案做為資料圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="e411c-172">這需要 XML 檔案也具有結構描述檔案 (*.xsd*檔案) 的 XML 結構描述。</span><span class="sxs-lookup"><span data-stu-id="e411c-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="e411c-173">此程序會示範如何從 XML 檔案讀取資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="e411c-174">在*應用程式\_資料*資料夾中，建立新的 XML 檔案，名為*data.xml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="e411c-175">下列程式碼，也就是一些有關虛構公司中員工的 XML 資料取代現有的 XML。</span><span class="sxs-lookup"><span data-stu-id="e411c-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="e411c-176">在*應用程式\_資料*資料夾中，建立新的 XML 檔案，名為*data.xsd*。</span><span class="sxs-lookup"><span data-stu-id="e411c-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="e411c-177">(請注意，擴充功能目前是*.xsd*。)</span><span class="sxs-lookup"><span data-stu-id="e411c-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="e411c-178">以下列內容取代現有的 XML:</span><span class="sxs-lookup"><span data-stu-id="e411c-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="e411c-179">在網站的根目錄，在建立新的檔案，名為*ChartDataXML.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="e411c-180">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="e411c-181">程式碼會先建立`DataSet`物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="e411c-182">此物件用來管理的資料會從 XML 檔案讀取，並組織根據結構描述檔案中的資訊。</span><span class="sxs-lookup"><span data-stu-id="e411c-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="e411c-183">(請注意程式碼的頂端包含陳述式`using SystemData`。</span><span class="sxs-lookup"><span data-stu-id="e411c-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="e411c-184">這為了能夠使用必要`DataSet`物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="e411c-185">如需詳細資訊，請參閱[&quot;使用&quot;陳述式和完整名稱](#SB_UsingStatements)本文稍後。)</span><span class="sxs-lookup"><span data-stu-id="e411c-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="e411c-186">接下來，程式碼建立`DataView`資料集為基礎的物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="e411c-187">資料檢視提供的物件，圖表可以繫結至&#8212;亦即，讀取和繪製。</span><span class="sxs-lookup"><span data-stu-id="e411c-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="e411c-188">圖表會繫結至資料使用`AddSeries`方法，為您稍早時看見圖表陣列資料，但這次`xValue`和`yValues`參數已設為`DataView`物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="e411c-189">此範例也示範如何指定特定圖表類型。</span><span class="sxs-lookup"><span data-stu-id="e411c-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="e411c-190">在 加入資料時`AddSeries`方法，`chartType`參數也會設為顯示圓形圖。</span><span class="sxs-lookup"><span data-stu-id="e411c-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="e411c-191">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="e411c-192">"Using"陳述式和完整限定的名稱</span><span class="sxs-lookup"><span data-stu-id="e411c-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="e411c-193">含有 Razor 語法的 ASP.NET Web Pages 根據.NET Framework 包含數千個元件 （類別）。</span><span class="sxs-lookup"><span data-stu-id="e411c-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="e411c-194">若要使用所有這些類別可管理，它們組織成*命名空間*，其中會有些許類似文件庫。</span><span class="sxs-lookup"><span data-stu-id="e411c-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="e411c-195">例如，`System.Web`命名空間包含類別，可支援瀏覽器/伺服器通訊，`System.Xml`命名空間包含類別，可用來建立和讀取 XML 檔案和`System.Data`命名空間包含類別，可讓您處理與資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="e411c-196">若要存取.NET Framework 中的任何特定的類別，程式碼需要知道不只是類別名稱，但也表示類別仍在命名空間。</span><span class="sxs-lookup"><span data-stu-id="e411c-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="e411c-197">例如，若要使用`Chart`helper，若要尋找的程式碼需要`System.Web.Helpers.Chart`類別，其結合了命名空間 (`System.Web.Helpers`) 與類別名稱 (`Chart`)。</span><span class="sxs-lookup"><span data-stu-id="e411c-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="e411c-198">這就所謂的類別*完整*名稱&#8212;其完整的明確位置 vastness 內的.NET framework。</span><span class="sxs-lookup"><span data-stu-id="e411c-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="e411c-199">在程式碼，這看起來會如下所示：</span><span class="sxs-lookup"><span data-stu-id="e411c-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="e411c-200">不過，它很麻煩 （又容易出錯） 需要使用這些長、 完整的名稱，每次您想要參考類別或協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="e411c-201">因此，若要更輕鬆地使用類別名稱，您可以*匯入*的命名空間您感興趣，通常是只從.NET Framework 中的多個命名空間中的少數。</span><span class="sxs-lookup"><span data-stu-id="e411c-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="e411c-202">如果您已匯入命名空間，您可以使用只有類別名稱 (`Chart`) 而不是完整限定名稱 (`System.Web.Helpers.Chart`)。</span><span class="sxs-lookup"><span data-stu-id="e411c-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="e411c-203">當您的程式碼執行，且遇到類別名稱時，它看起來可能只匯入到該類別的命名空間中。</span><span class="sxs-lookup"><span data-stu-id="e411c-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="e411c-204">當您使用含有 Razor 語法的 ASP.NET Web Pages 建立網頁時，您通常使用相同的一組類別，每次包括`WebPage`類別、 各種 helper，等等。</span><span class="sxs-lookup"><span data-stu-id="e411c-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="e411c-205">若要儲存匯入相關的命名空間，每當您建立網站的工作，會將 ASP.NET 設定讓它自動匯入的每個網站的核心命名空間的一組。</span><span class="sxs-lookup"><span data-stu-id="e411c-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="e411c-206">這就是為什麼您沒處理命名空間或匯入到現在;您已使用過的所有類別都都會為您已經匯入的命名空間中。</span><span class="sxs-lookup"><span data-stu-id="e411c-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="e411c-207">不過，有時候您需要使用不會自動為您匯入的命名空間中的類別。</span><span class="sxs-lookup"><span data-stu-id="e411c-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="e411c-208">在此情況下，您可以使用該類別的完整名稱，或您手動匯入的命名空間包含的類別。</span><span class="sxs-lookup"><span data-stu-id="e411c-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="e411c-209">若要匯入命名空間，您使用`using`陳述式 (`import`在 Visual Basic 中)，如您之前在範例中看到文件。</span><span class="sxs-lookup"><span data-stu-id="e411c-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="e411c-210">例如，`DataSet`類別位於`System.Data`命名空間。</span><span class="sxs-lookup"><span data-stu-id="e411c-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="e411c-211">`System.Data`命名空間不會自動提供給 ASP.NET Razor 頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="e411c-212">因此，若要使用`DataSet`類別使用的完整限定的名稱，您可以使用類似的程式碼：</span><span class="sxs-lookup"><span data-stu-id="e411c-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="e411c-213">如果您必須使用`DataSet`重複類別您可以匯入像這樣的命名空間，然後使用程式碼中的 類別名稱：</span><span class="sxs-lookup"><span data-stu-id="e411c-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="e411c-214">您可以加入`using`陳述式，針對您想要參考的任何其他.NET Framework 命名空間。</span><span class="sxs-lookup"><span data-stu-id="e411c-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="e411c-215">不過，如所述，您不需要這種做法，因為會自動匯入 asp.net 中使用的命名空間中大部分的類別，您將使用*.cshtml*和*.vbhtml*頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>


<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="e411c-216">顯示在網頁內的圖表</span><span class="sxs-lookup"><span data-stu-id="e411c-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="e411c-217">在範例中您看到了到目前為止，您建立圖表，然後圖表直接瀏覽器中呈現為圖形。</span><span class="sxs-lookup"><span data-stu-id="e411c-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="e411c-218">在許多情況下，不過，您想要顯示圖表 頁面上，不只是單獨使用瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="e411c-219">若要這樣做，需要兩步驟程序。</span><span class="sxs-lookup"><span data-stu-id="e411c-219">To do that requires a two-step process.</span></span> <span data-ttu-id="e411c-220">第一個步驟是建立一個頁面，會產生圖表，您已經看到。</span><span class="sxs-lookup"><span data-stu-id="e411c-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="e411c-221">第二個步驟是在另一個網頁中顯示所產生的影像。</span><span class="sxs-lookup"><span data-stu-id="e411c-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="e411c-222">若要顯示的映像，您可以使用 HTML`<img>`中相同的項目，您通常用來顯示任何映像的方式。</span><span class="sxs-lookup"><span data-stu-id="e411c-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="e411c-223">不過，而不是參考*.jpg*或*.png*檔案，`<img>`項目參考*.cshtml*檔案，其中包含`Chart`helper，建立的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="e411c-224">顯示頁面的執行時，則`<img>`元素取得的輸出`Chart`協助程式，並轉譯圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="e411c-225">建立名為*ShowChart.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="e411c-226">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="e411c-227">程式碼會使用`<img>`項目以顯示您稍早建立的圖表*ChartArrayBasic.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="e411c-228">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-228">Run the web page in a browser.</span></span> <span data-ttu-id="e411c-229">*ShowChart.cshtml*檔案會顯示包含在程式碼為基礎的圖表影像*ChartArrayBasic.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="e411c-230">設定圖表的樣式</span><span class="sxs-lookup"><span data-stu-id="e411c-230">Styling a Chart</span></span>

<span data-ttu-id="e411c-231">`Chart`協助程式支援大量的選項可讓您自訂圖表的外觀。</span><span class="sxs-lookup"><span data-stu-id="e411c-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="e411c-232">您可以設定色彩、 字型、 框線和等等。</span><span class="sxs-lookup"><span data-stu-id="e411c-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="e411c-233">若要自訂圖表外觀的簡單方法是使用*佈景主題*。</span><span class="sxs-lookup"><span data-stu-id="e411c-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="e411c-234">佈景主題是一個資訊集合，指定如何呈現圖表使用的字型、 色彩、 標籤、 調色盤、 框線和效果。</span><span class="sxs-lookup"><span data-stu-id="e411c-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="e411c-235">（請注意圖表樣式的圖表類型不表示）。</span><span class="sxs-lookup"><span data-stu-id="e411c-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="e411c-236">下表列出內建的佈景主題。</span><span class="sxs-lookup"><span data-stu-id="e411c-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="e411c-237">主題</span><span class="sxs-lookup"><span data-stu-id="e411c-237">Theme</span></span> | <span data-ttu-id="e411c-238">描述</span><span class="sxs-lookup"><span data-stu-id="e411c-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="e411c-239">白色背景上顯示紅色的資料行。</span><span class="sxs-lookup"><span data-stu-id="e411c-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="e411c-240">顯示藍色藍色漸層背景上的資料行。</span><span class="sxs-lookup"><span data-stu-id="e411c-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="e411c-241">顯示藍色綠色漸層背景上的資料行。</span><span class="sxs-lookup"><span data-stu-id="e411c-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="e411c-242">黃色漸層背景上顯示橙色的資料行。</span><span class="sxs-lookup"><span data-stu-id="e411c-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="e411c-243">白色背景上顯示 3d 紅色的資料行。</span><span class="sxs-lookup"><span data-stu-id="e411c-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="e411c-244">您可以指定要使用當您建立新圖表的佈景主題。</span><span class="sxs-lookup"><span data-stu-id="e411c-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="e411c-245">建立新的檔案命名為*ChartStyleGreen.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="e411c-246">以下列內容取代現有的內容頁面中：</span><span class="sxs-lookup"><span data-stu-id="e411c-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="e411c-247">此程式碼相當於先前的範例會使用資料庫的資料，但將`theme`參數會在建立時`Chart`物件。</span><span class="sxs-lookup"><span data-stu-id="e411c-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="e411c-248">下圖顯示變更的程式碼：</span><span class="sxs-lookup"><span data-stu-id="e411c-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="e411c-249">執行網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-249">Run the page in a browser.</span></span> <span data-ttu-id="e411c-250">您會看到相同的資料之前，但圖表看起來更精美：</span><span class="sxs-lookup"><span data-stu-id="e411c-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="e411c-251">儲存圖表</span><span class="sxs-lookup"><span data-stu-id="e411c-251">Saving a Chart</span></span>

<span data-ttu-id="e411c-252">當您使用`Chart`協助程式為您所知本文中，協助重新建立的圖表從頭每次叫用時。</span><span class="sxs-lookup"><span data-stu-id="e411c-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="e411c-253">必要時，圖表的程式碼也重新查詢資料庫，或重新讀取 XML 檔案，以取得資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="e411c-254">在某些情況下，這個動作可能複雜的作業，例如，如果您要查詢的資料庫很大，或 XML 檔案包含大量資料。</span><span class="sxs-lookup"><span data-stu-id="e411c-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="e411c-255">即使圖表不會包含大量資料，以動態方式建立映像的程序佔用伺服器資源，若有許多人要求的頁面顯示圖表，則可能會影響您的網站的效能。</span><span class="sxs-lookup"><span data-stu-id="e411c-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="e411c-256">為了協助您降低建立圖表的潛在效能影響，您可以建立圖表第一次需要它，並將其儲存。</span><span class="sxs-lookup"><span data-stu-id="e411c-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="e411c-257">當圖表所需一次，而不是重新產生，您也可以擷取儲存的版本，並呈現該。</span><span class="sxs-lookup"><span data-stu-id="e411c-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="e411c-258">您可以以下列方式來儲存圖表：</span><span class="sxs-lookup"><span data-stu-id="e411c-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="e411c-259">快取 （伺服器） 上的電腦記憶體中的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="e411c-260">將圖表儲存為影像檔。</span><span class="sxs-lookup"><span data-stu-id="e411c-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="e411c-261">將圖表儲存為 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-261">Save the chart as an XML file.</span></span> <span data-ttu-id="e411c-262">此選項可讓您儲存之前，修改圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="e411c-263">快取圖表</span><span class="sxs-lookup"><span data-stu-id="e411c-263">Caching a Chart</span></span>

<span data-ttu-id="e411c-264">在您建立圖表後，您可以快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="e411c-265">快取圖表，以表示它並未在重新建立，需要再次顯示。</span><span class="sxs-lookup"><span data-stu-id="e411c-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="e411c-266">當您將圖表儲存快取中時，您指定不得重複用於該圖表的索引鍵。</span><span class="sxs-lookup"><span data-stu-id="e411c-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="e411c-267">如果在伺服器記憶體不足，可能會移除儲存在快取的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="e411c-268">此外，如果因為任何原因而重新啟動您的應用程式，會清除快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="e411c-269">因此，標準的方式來處理快取的圖表是以一律先查看是否有快取中，以及如果不是，則建立或重新建立它。</span><span class="sxs-lookup"><span data-stu-id="e411c-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="e411c-270">在您的網站根目錄中，建立名為*ShowCachedChart.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="e411c-271">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="e411c-272">`<img>`標記包含`src`屬性指向*ChartSaveToCache.cshtml*檔案，並將金鑰傳遞給查詢字串為頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="e411c-273">此金鑰包含值&quot;myChartKey&quot;。</span><span class="sxs-lookup"><span data-stu-id="e411c-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="e411c-274">*ChartSaveToCache.cshtml*檔案包含`Chart`helper 所建立的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="e411c-275">您要立即建立這個頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="e411c-276">在頁面的結尾沒有名為頁面的連結*ClearCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="e411c-277">這是您也會短時間內建立的頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="e411c-278">您需要*ClearCache.cshtml*僅要測試此範例中的快取，它不是連結或使用快取的圖表時，通常會包含的頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="e411c-279">在您的網站根目錄中，建立新的檔案命名為*ChartSaveToCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="e411c-280">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="e411c-281">程式碼首先檢查任何項目是否已傳遞做為查詢字串中的索引鍵值。</span><span class="sxs-lookup"><span data-stu-id="e411c-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="e411c-282">如果這個程式碼，會嘗試藉由呼叫讀取快取圖表`GetFromCache`方法，並將其傳遞索引鍵。</span><span class="sxs-lookup"><span data-stu-id="e411c-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="e411c-283">如果證明，沒有在該索引鍵 （這會發生第一次要求圖表） 快取中的項目，程式碼會如往常般建立圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="e411c-284">圖表完成時，程式碼將它儲存至快取藉由呼叫`SaveToCache`。</span><span class="sxs-lookup"><span data-stu-id="e411c-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="e411c-285">該方法需要索引鍵 （因此稍後可以要求圖表） 和圖表應儲存在快取中的時間量。</span><span class="sxs-lookup"><span data-stu-id="e411c-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="e411c-286">（您會快取圖表的確切時間會取決於您認為它所代表的資料可能變更的頻率）。`SaveToCache`方法也需要`slidingExpiration`參數&#8212;如果設定為 true，在逾時計數器會重設每次存取時的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="e411c-287">在此情況下，其作用中表示圖表的快取項目過期有人存取圖表上一次之後的 2 分鐘。</span><span class="sxs-lookup"><span data-stu-id="e411c-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="e411c-288">（滑動期限的替代方式是絕對期限，表示快取項目會到期完全 2 分鐘後它已放入快取中，不論其頻率必須存取）。</span><span class="sxs-lookup"><span data-stu-id="e411c-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="e411c-289">最後，程式碼會使用`WriteFromCache`方法來擷取和轉譯來自快取的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="e411c-290">請注意，這個方法是外部`if`檢查快取，因為圖表已有一開始還是必須產生並儲存在快取，它會從快取取得圖表的區塊。</span><span class="sxs-lookup"><span data-stu-id="e411c-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="e411c-291">請注意，在範例中，`AddTitle`方法包含時間戳記。</span><span class="sxs-lookup"><span data-stu-id="e411c-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="e411c-292">(它會將目前的日期和時間&#8212; `DateTime.Now` &#8212;標題。)</span><span class="sxs-lookup"><span data-stu-id="e411c-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="e411c-293">建立名為的新頁面*ClearCache.cshtml*和其內容取代為下列：</span><span class="sxs-lookup"><span data-stu-id="e411c-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="e411c-294">此頁面會使用`WebCache`移除圖表中快取*ChartSaveToCache.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="e411c-295">如前文所述，您通常不必有一個像這樣的頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="e411c-296">您要建立，請參閱這裡只以更輕鬆地測試快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="e411c-297">執行*ShowCachedChart.cshtml*網頁瀏覽器中。</span><span class="sxs-lookup"><span data-stu-id="e411c-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="e411c-298">此頁面會顯示包含在程式碼為基礎的圖表影像*ChartSaveToCache.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="e411c-299">請注意圖表標題中顯示的時間戳記。</span><span class="sxs-lookup"><span data-stu-id="e411c-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![描述： 時間戳記在圖表標題的基本圖圖片](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="e411c-301">關閉瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="e411c-301">Close the browser.</span></span>
8. <span data-ttu-id="e411c-302">執行*ShowCachedChart.cshtml*一次。</span><span class="sxs-lookup"><span data-stu-id="e411c-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="e411c-303">請注意，時間戳記相同如往常一般，表示圖表未重新產生，但改為從快取中讀取。</span><span class="sxs-lookup"><span data-stu-id="e411c-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="e411c-304">在*ShowCachedChart.cshtml*，按一下 **清除快取**連結。</span><span class="sxs-lookup"><span data-stu-id="e411c-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="e411c-305">這會帶您前往*ClearCache.cshtml*，哪些報告已清除快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="e411c-306">按一下**返回 ShowCachedChart.cshtml**連結，或重新執行*ShowCachedChart.cshtml*從 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="e411c-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="e411c-307">請注意，此時間戳記已變更，因為在清除快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="e411c-308">因此，程式碼必須重新產生圖表，並放回快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="e411c-309">將圖表儲存為影像檔</span><span class="sxs-lookup"><span data-stu-id="e411c-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="e411c-310">您也可以將圖表儲存為影像檔 (例如，做為*.jpg*檔案) 的伺服器上。</span><span class="sxs-lookup"><span data-stu-id="e411c-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="e411c-311">就像任何映像的方式時，可以使用映像檔。</span><span class="sxs-lookup"><span data-stu-id="e411c-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="e411c-312">優點是，檔案會儲存，而不是儲存在暫時快取。</span><span class="sxs-lookup"><span data-stu-id="e411c-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="e411c-313">您可以將新的圖表影像儲存在不同的時間 （例如，每小時），並保持永久記錄一段時間的變更。</span><span class="sxs-lookup"><span data-stu-id="e411c-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="e411c-314">請注意，您必須先確定您的 web 應用程式已將檔案儲存到資料夾中，您要將影像檔放在伺服器上的權限。</span><span class="sxs-lookup"><span data-stu-id="e411c-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="e411c-315">在您的網站根目錄中，建立名為 *\_ChartFiles*如果不存在。</span><span class="sxs-lookup"><span data-stu-id="e411c-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="e411c-316">在您的網站根目錄中，建立新的檔案命名為*ChartSave.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="e411c-317">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="e411c-318">程式碼會先檢查以查看是否*.jpg*檔案存在，藉由呼叫`File.Exists`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="e411c-319">如果檔案不存在，程式碼會建立新`Chart`從陣列。</span><span class="sxs-lookup"><span data-stu-id="e411c-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="e411c-320">此時，程式碼會呼叫`Save`方法，並傳遞`path`參數來指定的檔案路徑和檔案名稱儲存圖表的位置。</span><span class="sxs-lookup"><span data-stu-id="e411c-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="e411c-321">本文的頁面上，`<img>`項目使用的路徑指向*.jpg*檔案，即可顯示。</span><span class="sxs-lookup"><span data-stu-id="e411c-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="e411c-322">執行*ChartSave.cshtml*檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="e411c-323">傳回至 WebMatrix。</span><span class="sxs-lookup"><span data-stu-id="e411c-323">Return to WebMatrix.</span></span> <span data-ttu-id="e411c-324">請注意，映像檔名為*chart01.jpg*就已經儲存在 *\_ChartFiles*資料夾。</span><span class="sxs-lookup"><span data-stu-id="e411c-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="e411c-325">將圖表儲存為 XML 檔案</span><span class="sxs-lookup"><span data-stu-id="e411c-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="e411c-326">最後，您可以將圖表儲存為伺服器上的 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="e411c-327">使用此方法優於快取圖表或將圖表儲存至檔案的優點是您無法再顯示圖表，如果您想要修改的 XML。</span><span class="sxs-lookup"><span data-stu-id="e411c-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="e411c-328">您的應用程式必須要有您要將影像檔放在伺服器上資料夾的讀取/寫入權限。</span><span class="sxs-lookup"><span data-stu-id="e411c-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="e411c-329">在您的網站根目錄中，建立新的檔案命名為*ChartSaveXml.cshtml*。</span><span class="sxs-lookup"><span data-stu-id="e411c-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="e411c-330">以下列內容取代現有的內容：</span><span class="sxs-lookup"><span data-stu-id="e411c-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="e411c-331">此程式碼很類似之前看到將圖表儲存在快取中，不同之處在於它會使用 XML 檔案的程式碼。</span><span class="sxs-lookup"><span data-stu-id="e411c-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="e411c-332">程式碼會先檢查以查看 XML 檔案是否存在藉由呼叫`File.Exists`方法。</span><span class="sxs-lookup"><span data-stu-id="e411c-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="e411c-333">如果檔案不存在，程式碼會建立新`Chart`物件，並傳遞的檔案名稱為`themePath`參數。</span><span class="sxs-lookup"><span data-stu-id="e411c-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="e411c-334">這會建立基礎無論是 XML 檔案中的圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="e411c-335">如果 XML 檔案不存在，程式碼建立的圖表，以正常的方式，然後呼叫`SaveXml`儲存它。</span><span class="sxs-lookup"><span data-stu-id="e411c-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="e411c-336">使用轉譯圖表`Write`方法，為您所見之前。</span><span class="sxs-lookup"><span data-stu-id="e411c-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="e411c-337">如同的快取的頁面上，這段程式碼會在圖表標題中包含時間戳記。</span><span class="sxs-lookup"><span data-stu-id="e411c-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="e411c-338">建立名為的新頁面*ChartDisplayXMLChart.cshtml*並加入下列標記：</span><span class="sxs-lookup"><span data-stu-id="e411c-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="e411c-339">執行*ChartDisplayXMLChart.cshtml*頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="e411c-340">圖表會顯示。</span><span class="sxs-lookup"><span data-stu-id="e411c-340">The chart is displayed.</span></span> <span data-ttu-id="e411c-341">記下的時間戳記在圖表的標題。</span><span class="sxs-lookup"><span data-stu-id="e411c-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="e411c-342">關閉瀏覽器。</span><span class="sxs-lookup"><span data-stu-id="e411c-342">Close the browser.</span></span>
6. <span data-ttu-id="e411c-343">在 WebMatrix 中，以滑鼠右鍵按一下 *\_ChartFiles*資料夾中，按一下 **重新整理**，，然後開啟資料夾。</span><span class="sxs-lookup"><span data-stu-id="e411c-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="e411c-344">*XMLChart.xml*此資料夾中的檔案由`Chart`協助程式。</span><span class="sxs-lookup"><span data-stu-id="e411c-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![描述： _ChartFiles 資料夾顯示圖表協助程式 」 所建立的 XMLChart.xml 檔案。](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="e411c-346">執行*ChartDisplayXMLChart.cshtml*頁面上一次。</span><span class="sxs-lookup"><span data-stu-id="e411c-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="e411c-347">圖表可顯示相同的時間戳記，當做第一次執行的頁面。</span><span class="sxs-lookup"><span data-stu-id="e411c-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="e411c-348">這是因為正在從您稍早儲存的 XML 產生圖表。</span><span class="sxs-lookup"><span data-stu-id="e411c-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="e411c-349">在 WebMatrix 中開啟 *\_ChartFiles*資料夾，然後刪除*XMLChart.xml*檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="e411c-350">執行*ChartDisplayXMLChart.cshtml*頁面上一次。</span><span class="sxs-lookup"><span data-stu-id="e411c-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="e411c-351">這次更新時間戳記，因為`Chart`協助程式必須重新建立 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="e411c-352">如果您想要請檢查 *\_ChartFiles*資料夾，然後注意上一步是 XML 檔案。</span><span class="sxs-lookup"><span data-stu-id="e411c-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="e411c-353">其他資源</span><span class="sxs-lookup"><span data-stu-id="e411c-353">Additional Resources</span></span>

- [<span data-ttu-id="e411c-354">簡介使用的資料庫中 ASP.NET Web Pages 站台</span><span class="sxs-lookup"><span data-stu-id="e411c-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="e411c-355">使用快取中 ASP.NET Web Pages 站台，以改善效能</span><span class="sxs-lookup"><span data-stu-id="e411c-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="e411c-356">[圖表類別](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99))（MSDN 上的 ASP.NET Web Pages 應用程式開發介面參考）</span><span class="sxs-lookup"><span data-stu-id="e411c-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
