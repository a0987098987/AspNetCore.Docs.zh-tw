---
uid: web-forms/overview/moving-to-aspnet-20/data-bound-controls
title: "資料繫結控制項 |Microsoft 文件"
author: microsoft
description: "大部分的 ASP.NET 應用程式依賴某種程度的後端資料來源的資料簡報。 資料繫結控制項有關鍵的互動 w..."
ms.author: aspnetcontent
manager: wpickett
ms.date: 02/20/2005
ms.topic: article
ms.assetid: 0e23ff32-646d-43f3-8bec-6b2313d3abd6
ms.technology: dotnet-webforms
ms.prod: .net-framework
msc.legacyurl: /web-forms/overview/moving-to-aspnet-20/data-bound-controls
msc.type: authoredcontent
ms.openlocfilehash: 3ebb0f9a7a2f071b7bf7aa3855920f1a5784a61f
ms.sourcegitcommit: 9a9483aceb34591c97451997036a9120c3fe2baf
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 11/10/2017
---
<a name="data-bound-controls"></a>資料繫結控制項
====================
由[Microsoft](https://github.com/microsoft)

> 大部分的 ASP.NET 應用程式依賴某種程度的後端資料來源的資料簡報。 資料繫結控制項已動態 Web 應用程式中的資料互動的關鍵部分。 ASP.NET 2.0 導入了一些大幅改善，用於資料繫結控制項，包括新 BaseDataBoundControl 類別，與宣告式語法。


大部分的 ASP.NET 應用程式依賴某種程度的後端資料來源的資料簡報。 資料繫結控制項已動態 Web 應用程式中的資料互動的關鍵部分。 ASP.NET 2.0 導入了一些大幅改善，用於資料繫結控制項，包括新 BaseDataBoundControl 類別，與宣告式語法。

BaseDataBoundControl 做為 DataBoundControl 類別和 HierarchicalDataBoundControl 類別的基底類別。 在此模組中，我們將討論下列類別衍生自 DataBoundControl:

- AdRotator
- 清單控制項
- GridView
- 在 FormView
- 在 DetailsView

我們也將討論下列類別衍生自 HierarchicalDataBoundControl 類別：

- TreeView
- 功能表
- SiteMapPath

## <a name="databoundcontrol-class"></a>DataBoundControl 類別

DataBoundControl 類別是用來與表格式互動的抽象類別 （標示 MustInherit VB 中的） 或清單樣式的資料。 下列控制項是一些衍生自 DataBoundControl 的控制項。

## <a name="adrotator"></a>AdRotator

AdRotator 控制項可讓您連結至特定 URL 的 Web 網頁上顯示橫幅圖形。 旋轉圖形顯示使用控制項的屬性。 您可以使用設定的頁面上顯示的特定 ad 頻率**感想**屬性廣告可以篩選及使用關鍵字篩選。

AdRotator 控制項使用的 XML 檔案或資料表的資料庫中的資料。 下列屬性可用在 XML 檔案來設定 AdRotator 控制項。

### <a name="imageurl"></a>ImageUrl
若要顯示的廣告的影像 URL。

### <a name="navigateurl"></a>NavigateUrl
按一下廣告時，使用者應該要採取的 URL。 這應該是 URL 編碼。

### <a name="alternatetext"></a>AlternateText
替代文字是顯示在工具提示及螢幕助讀程式讀取。 無法使用 ImageUrl 所指定的影像時也會顯示。

### <a name="keyword"></a>關鍵字
定義可以使用關鍵字篩選時使用的關鍵字。 如果指定，就會顯示這些廣告以符合關鍵字篩選器的關鍵字。

### <a name="impressions"></a>感想
判斷特定 ad 可能出現的頻率加權數字。 它是相對於同一個檔案中的其他廣告的印象。 集合的感想 XML 檔案中的所有廣告的最大值為 2,048,000,000 1。

### <a name="height"></a>高度
廣告像素為單位的高度。

### <a name="width"></a>寬度
單位為像素廣告的寬度。


> [!NOTE]
> 高度和寬度屬性覆寫的高度和寬度 AdRotator 控制項本身。


典型的 XML 檔案看起來可能如下所示：

[!code-xml[Main](data-bound-controls/samples/sample1.xml)]

在上述範例中，contoso ad 兩次，可能會因為感想屬性的值會顯示為 ASP.NET 網頁站台的 ad。

若要顯示廣告從上述的 XML 檔案，請將 AdRotator 控制項加入頁面並設定**AdvertisementFile**屬性以指向的 XML 檔案，如下所示：

[!code-aspx[Main](data-bound-controls/samples/sample2.aspx)]

如果您選擇要做為資料來源的資料庫資料表用於 AdRotator 控制項，您必須使用下列結構描述的資料庫設定：

| **資料行名稱** | **資料類型** | **說明** |
| --- | --- | --- |
| ID | int | 主索引鍵。 此資料行可以具有任何名稱。 |
| ImageUrl | nvarchar (*長度*) | 相對或絕對的影像 URL 来顯示的廣告。 |
| NavigateUrl | nvarchar (*長度*) | Ad 目標 URL。 如果您未提供值，ad 不是超連結。 |
| AlternateText | nvarchar (*長度*) | 如果找不到映像所顯示的文字。 在某些瀏覽器中，會顯示為工具提示的文字。 替代文字是也用來協助工具，讓使用者能夠看圖形聽見大聲地讀出來閱讀其說明。 |
| 關鍵字 | nvarchar (*長度*) | 頁面上可以篩選在其上的廣告類別。 |
| 感想 | int （4) | 數字，指出廣告顯示頻率的可能性。 較大的數目，越常 ad 將會顯示。 XML 檔案中的所有感想值的總和不得超過 2,048,000,000-1。 |
| 寬度 | int （4) | 單位為像素影像的寬度。 |
| 高度 | int （4) | 單位為像素影像的高度。 |

在已經有具有不同的結構描述的資料庫的情況下，您可以使用**AlternateTextField**， **ImageUrlField**，和**NavigateUrlField**可對應的屬性AdRotator 屬性，將您現有的資料庫。 若要在 AdRotator 控制項中顯示資料庫中的資料，加入網頁中的資料來源控制項、 設定資料來源控制項，以指向您資料庫的連接字串及設定 AdRotator 控制項**DataSourceID**資料來源控制項的 id 屬性。 在其中您需要以程式設計方式設定 AdRotator 廣告的情況下，使用 AdCreated 事件。 AdCreated 事件會採用兩個參數。其中一個物件，以及其他 AdCreatedEventArgs 的執行個體。 AdCreatedEventArgs 是正在建立 ad 的參考。

下列程式碼片段的 ImageUrl、 NavigateUrl 和 AlternateText ad 以程式設計方式設定：

[!code-csharp[Main](data-bound-controls/samples/sample3.cs)]

## <a name="list-controls"></a>清單控制項

清單控制項包含清單方塊、 DropDownList、 CheckBoxList、 RadioButtonList 及設定 BulletedList。 這些控制項可以是資料繫結至資料來源。 這些資料來源中的一個欄位做的顯示文字，並可以選擇性地使用項目的值為第二個欄位。 項目也可以加入以靜態方式在設計階段，您可以混合靜態項目和動態資料來源中加入的項目。

資料繫結清單控制項，加入網頁中的資料來源控制項。 指定資料來源控制項的選取命令，然後將清單控制項中的 [DataSourceID] 屬性設定為資料來源控制項的 ID。 使用**DataTextField**和**DataValueField**屬性來定義的顯示文字和控制項的值。 此外，您可以使用**DataTextFormatString**屬性來控制顯示文字的外觀，如下所示：

| **運算式** | **說明** |
| --- | --- |
| Price: {0:C} | 針對數值/十進位資料。 顯示常值"Price: 」 後面的貨幣格式的數字。 貨幣格式取決於在文化特性屬性中指定的文化特性設定**頁面**指示詞或 Web.config 檔案中。 |
| {0:D4} | 適用於整數資料。 不能與十進位數字。 整數被顯示在以零填補的欄位有四個字元寬。 |
| {0:N2}% | 針對數值資料。 顯示 2 位小數位數的數字精確度其後為常值"%"。 |
| {0:000.0} | 針對數值/十進位資料。 數字會四捨五入到一個小數位數。 小於三位數的數字會填補零。 |
| {0:D} | 針對日期/時間資料。 顯示完整日期格式 ("星期四，August 06，1996")。 日期格式取決於頁面或 Web.config 檔案的文化設定。 |
| {0:d} | 針對日期/時間資料。 顯示簡短日期格式 ("12/31/99")。 |
| {0:yy-MM-dd} | 針對日期/時間資料。 數值年-月-日格式 (96-08-06) 中顯示日期 |

## <a name="gridview"></a>GridView

GridView 控制項允許的表格式資料顯示和編輯使用宣告式方法，而且是 DataGrid 控制項中的後續版本。 在 GridView 控制項中使用下列功能。

- 繫結至資料來源控制項，例如 SqlDataSource。
- 內建的排序功能。
- 內建更新和刪除功能。
- 內建的分頁功能。
- 內建的資料列選取功能。
- 以程式設計方式存取 GridView 物件模型，來動態設定屬性，處理事件，等等。
- 多個索引鍵欄位。
- 超連結資料行的多個資料欄位。
- 透過佈景主題和樣式的可自訂外觀。

**資料行欄位**

DataControlField 物件所代表 GridView 控制項中的每個資料行。 根據預設，AutoGenerateColumns 屬性設定為**true**，其資料來源中建立每個欄位的 AutoGeneratedField 物件。 接著會將每個欄位轉譯為每個欄位會出現在資料來源的順序的 GridView 控制項中的資料行。 您可以手動控制欄位會出現在哪一個資料行**GridView**藉由設定控制**AutoGenerateColumns**屬性**false**然後定義您自己資料行的欄位集合。 不同的資料行欄位的類型決定控制項中的資料行的行為。

下表列出可用的不同的資料行欄位型別。

| **資料行欄位類型** | **說明** |
| --- | --- |
| BoundField | 顯示資料來源中的欄位的值。 這是預設資料行的 GridView 控制項類型。 |
| ButtonField | GridView 控制項中顯示每個項目在命令按鈕。 這可讓您建立的資料行的自訂按鈕控制項，例如 [新增] 或 [移除] 按鈕。 |
| CheckBoxField | GridView 控制項中顯示每個項目的核取方塊。 這個資料行的欄位型別通常用於顯示具有布林值的欄位。 |
| CommandField | 顯示預先定義的命令按鈕，可執行選取、 編輯或刪除作業。 |
| HyperLinkField | 資料來源中的欄位的值顯示為超連結。 這個資料行的欄位型別可讓您將第二個欄位繫結至超連結的 URL。 |
| ImageField | 在 GridView 控制項中顯示每個項目的影像。 |
| TemplateField | 顯示使用者定義內容根據指定的範本 GridView 控制項中的每個項目。 這個資料行的欄位型別可讓您建立自訂的資料行的欄位。 |

若要以宣告方式定義資料行的欄位集合，請先加入開啟和關閉**&lt;資料行&gt;**開頭和結尾標記的 GridView 控制項之間的標記。 接下來，列出您想要包含在開頭和結尾之間的資料行欄位**&lt;資料行&gt;**標記。 指定資料行加入資料行集合中列出的順序。 **資料行**集合會儲存所有資料行欄位控制項中，並可讓您以程式設計方式管理 GridView 控制項中的資料行欄位。

明確宣告的資料行欄位可顯示與自動產生的資料行欄位一起使用。 同時使用時，明確宣告資料行欄位會首先呈現，後面接著的自動產生的資料行欄位。

## <a name="binding-to-data"></a>繫結至資料

GridView 控制項可以繫結至資料來源控制項 (例如**SqlDataSource**， **ObjectDataSource**等等)，以及實作 System.Collections.IEnumerable 任何資料來源介面 （例如 System.Data.DataView、 System.Collections.ArrayList 或 System.Collections.Hashtable）。 使用下列方法之一將 GridView 控制項繫結至適當的資料來源類型：

- 若要繫結至資料來源控制項，設定 DataSourceID 屬性 GridView 控制項的資料來源控制項的識別碼值。 GridView 控制項自動繫結至指定的資料來源控制項，並可使用的資料來源控制項的功能，才能執行排序、 更新、 刪除和分頁功能。 這是慣用的方法繫結至資料。
- 若要繫結至資料來源實作 System.Collections.IEnumerable 介面，以程式設計方式設定資料來源的 GridView 控制項的 DataSource 屬性然後再呼叫 DataBind 方法。 使用此方法時，GridView 控制項不提供內建排序、 更新、 刪除和分頁功能。 您必須自行提供這項功能。

## <a name="data-operations"></a>資料作業

GridView 控制項提供許多的內建功能可讓使用者排序、 更新、 刪除、 選取及透過控制項中的項目頁面上。 GridView 控制項繫結至資料來源控制項，GridView 控制項可以讓您利用資料來源控制項的功能，並提供自動排序、 更新和刪除功能。

> [!NOTE]
> GridView 控制項可提供支援排序、 更新和刪除與其他類型的資料來源;不過，您必須提供適當的事件處理常式實作這些作業。


排序功能可以讓使用者的資料行標頭即可排序 GridView 控制項相對於特定資料行中的項目。 若要啟用排序，將 AllowSorting 屬性設定為**true**。

當功能的按鈕時，會啟用自動更新、 刪除和選取項目功能**ButtonField**或**TemplateField**資料行 欄位與 「 編輯 」、 「 刪除 」 及 「 選取 」 的命令名稱分別按下。 GridView 控制項可自動將新增**CommandField**資料行 欄位和編輯、 刪除或選取的按鈕，如果 autogenerateeditbutton、 autogeneratedeletebutton 或 AutoGenerateSelectButton 屬性設定為**true**分別。

> [!NOTE]
> 將資料錄插入到資料來源不直接支援由 GridView 控制項。 不過，很可能使用搭配 DetailsView 的 GridView 控制項或 FormView 控制項插入記錄。


而不是顯示資料來源中的所有記錄，在相同的時間，GridView 控制項可以自動分成記錄頁面。 若要啟用分頁，將 AllowPaging 屬性設定為**true**。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以設定控制項的不同部分的樣式屬性，來自訂 GridView 控制項的外觀。 下表列出不同的樣式屬性。

| **樣式屬性** | **說明** |
| --- | --- |
| AlternatingRowStyle | 在 GridView 控制項中的交替資料列樣式設定。 當設定這個屬性時，資料列會顯示交替 RowStyle 設定和**AlternatingRowStyle**設定。 |
| EditRowStyle | 編輯 GridView 控制項中的資料列樣式設定。 |
| EmptyDataRowStyle | 當資料來源未包含任何記錄 GridView 控制項中顯示空白資料列樣式設定。 |
| FooterStyle | GridView 控制項的頁尾資料列樣式設定。 |
| HeaderStyle | GridView 控制項的標頭資料列樣式設定。 |
| PagerStyle | 頁面巡覽列 GridView 控制項的樣式設定。 |
| RowStyle | GridView 控制項中的資料列樣式設定。 當**AlternatingRowStyle**屬性也設定時，資料列會顯示交替**RowStyle**設定和**AlternatingRowStyle**設定。 |
| SelectedRowStyle | 在 GridView 控制項中選取的資料列樣式設定。 |

您也可以顯示或隱藏的控制項的不同部分。 下表列出可控制哪些部分要顯示或隱藏的屬性。

| **Property** | **說明** |
| --- | --- |
| ShowFooter | 顯示或隱藏 GridView 控制項的頁尾區段。 |
| ShowHeader | 顯示或隱藏 GridView 控制項的標頭區段。 |

### <a name="events"></a>事件

GridView 控制項提供數個事件，您可以程式設計的。 這可讓您在事件發生時執行自訂的常式。 下表列出支援的 GridView 控制項的事件。

| **Event** | **說明** |
| --- | --- |
| PageIndexChanged | 發生於按下一個頁面巡覽區按鈕時，但之後 GridView 控制項控點的分頁作業。 當您需要執行的工作之後使用者巡覽至控制項中的不同頁面, 時，通常使用這個事件。 |
| PageIndexChanging | 發生於按下一個頁面巡覽區按鈕時，但之前 GridView 控制項負責的分頁作業。 此事件通常用於取消的分頁作業。 |
| RowCancelingEdit | 按一下資料列的 [取消] 按鈕時，但 GridView 控制項之前結束編輯模式時發生。 此事件通常用來停止取消作業。 |
| RowCommand | GridView 控制項中按下按鈕時，就會發生。 此事件通常用於在控制項中按下按鈕時執行工作。 |
| RowCreated | GridView 控制項中建立新的資料列時，就會發生。 若要修改的資料列的內容時建立的資料列通常使用這個事件。 |
| RowDataBound | 發生於資料列繫結至 GridView 控制項中的資料。 此事件通常用於資料繫結的資料列時修改資料列的內容。 |
| RowDeleted | 發生於按一下資料列的 刪除 按鈕時，但之後 GridView 控制項從 資料來源中刪除記錄。 此事件通常用來檢查刪除作業的結果。 |
| RowDeleting | 發生於按一下資料列的 [刪除] 按鈕時，但之前 GridView 控制項中刪除記錄從資料來源。 此事件通常用來取消刪除作業。 |
| RowEditing | 按一下資料列的 [編輯] 按鈕時，但之前 GridView 控制項進入編輯模式時發生。 此事件通常用來取消編輯作業。 |
| RowUpdated | 發生於按一下資料列的 [更新] 按鈕時，但在 GridView 控制項更新資料列。 若要檢查更新作業的結果通常使用這個事件。 |
| RowUpdating | 發生於按一下資料列的 [更新] 按鈕時，但之前 GridView 控制項更新資料列。 此事件通常用來取消更新作業。 |
| SelectedIndexChanged | 發生於按一下資料列選取按鈕時，但之後 GridView 控制項處理選取的作業。 在控制項中選取一個資料列之後執行的工作通常使用這個事件。 |
| SelectedIndexChanging | 發生於按一下資料列選取按鈕時，但之前 GridView 控制項處理選取的作業。 此事件通常用來取消選取作業。 |
| 排序 | 發生於按一下此超連結可排序資料行，但之後 GridView 控制項控點的排序作業。 此事件通常用於執行工作，在使用者按一下超連結以排序資料行之後。 |
| 排序 | 按一下此超連結可排序資料行，但之前 GridView 控制項負責排序作業時發生。 若要取消排序作業，或執行自訂排序常式，通常使用這個事件。 |

## <a name="formview"></a>在 FormView

在 FormView 控制項用來顯示資料來源的單一記錄。 很類似於 DetailsView 控制項，除了它會顯示使用者定義的範本，而不是資料列欄位。 建立您自己的範本，讓您更大的彈性來控制資料的顯示方式。 在 FormView 控制項支援下列功能：

- 繫結至資料來源控制項，例如 SqlDataSource 和 ObjectDataSource。
- 內建的插入功能。
- 內建更新和刪除功能。
- 內建的分頁功能。
- 以程式設計方式存取 FormView 物件模型，來動態設定屬性，處理事件，等等。
- 透過使用者定義的範本、 主題和樣式的可自訂外觀。

## <a name="templates"></a>範本

在 FormView 控制項來顯示內容，您需要建立控制項的不同部分的範本。 大部分的範本是選擇性的。不過，您必須建立的範本，以及設定控制項的模式。 例如，支援記錄插入 FormView 控制項必須有定義的插入項目範本。 下表列出您可以建立不同的範本。

| **範本類型** | **說明** |
| --- | --- |
| EditItemTemplate | FormView 控制項處於編輯模式時，請定義的資料列的內容。 此範本通常包含輸入的控制項和命令按鈕的使用者可以編輯現有的記錄。 |
| EmptyDataTemplate | 定義顯示 FormView 控制項繫結至資料來源不包含任何記錄時，空白資料列的內容。 此範本通常包含來提醒使用者資料來源不包含任何記錄的內容。 |
| FooterTemplate | 定義頁尾資料列的內容。 此範本通常包含您想要顯示頁尾資料列中的任何其他內容。 或者，您可以只指定 FooterText 屬性設定頁尾資料列中顯示的文字。 |
| HeaderTemplate | 定義標頭資料列的內容。 此範本通常包含您想要在標頭資料列中顯示的任何其他內容。 或者，您可以只指定要藉由設定 HeaderText 屬性標頭列中顯示的文字。 |
| ItemTemplate | FormView 控制項處於唯讀模式時，請定義的資料列的內容。 此範本通常包含用來顯示現有記錄的值。 |
| 上 | FormView 控制項處於插入模式時，請定義的資料列的內容。 此範本通常包含輸入的控制項和命令按鈕的使用者可以加入新的記錄。 |
| PagerTemplate | 定義啟用分頁功能時顯示的頁面巡覽列的內容 (當 AllowPaging 屬性設定為**true**)。 此範本通常包含的控制項的使用者可以瀏覽至另一筆記錄。 |

編輯項目樣板和插入項目樣板中的輸入的控制項可以繫結至資料來源的欄位使用雙向繫結運算式。 這可讓 FormView 控制項自動擷取更新的輸入控制項的值或插入作業。 雙向繫結運算式也可以讓輸入的控制項中自動顯示原始欄位值的編輯項目範本。

### <a name="binding-to-data"></a>繫結至資料

在 FormView 控制項可以繫結至資料來源控制項 (例如**SqlDataSource**，AccessDataSource， **ObjectDataSource**等等)，或實作任何資料來源System.Collections.IEnumerable 介面 （例如 System.Data.DataView、 System.Collections.ArrayList 和 System.Collections.Hashtable）。 您可以使用下列方法之一來 FormView 控制項繫結至適當的資料來源類型：

- 若要繫結至資料來源控制項，設定 DataSourceID 屬性 FormView 控制項的資料來源控制項的識別碼值。 在 FormView 控制項自動繫結至指定的資料來源控制項，並可以利用資料來源控制項的功能，以執行插入、 更新、 刪除和分頁功能。 這是慣用的方法繫結至資料。
- 繫結至資料來源實作**System.Collections.IEnumerable**介面、 FormView 控制項的 DataSource 屬性設定為資料來源，並接著呼叫進行資料繫結的方法。 使用此方法時，在 FormView 控制項不提供內建的插入、 更新、 刪除和分頁功能。 您需要提供此功能使用適當的事件。

## <a name="data-operations"></a>資料作業

在 FormView 控制項提供許多內建功能可讓使用者更新、 刪除、 插入和控制項中的項目頁面上。 FormView 控制項繫結至資料來源控制項，FormView 控制項可以讓您利用資料來源控制項的功能，並提供自動更新、 刪除、 插入和分頁功能。 在 FormView 控制項可以提供支援的 update、 delete、 insert 和分頁作業與其他類型的資料來源;不過，您必須提供適當的事件處理常式實作這些作業。

在 FormView 控制項使用的範本，因為它不提供自動產生命令按鈕來執行更新、 刪除或插入作業。 您必須手動加入這些命令按鈕，在適當的樣板。 在 FormView 控制項辨認某些按鈕具有其**CommandName**屬性設定為特定值。 下表列出 FormView 控制項辨認的命令按鈕。

| **Button** | **Commandname 值** | **說明** |
| --- | --- | --- |
| 取消 | [取消] | 取消作業，並捨棄使用者所輸入的值，更新或插入作業中使用。 在 FormView 控制項然後回到 DefaultMode 屬性所指定的模式。 |
| 刪除 | "Delete" | 用於刪除作業，以刪除資料來源中顯示的記錄。 引發 ItemDeleting 和 ItemDeleted 事件。 |
| Edit | [編輯] | 用於更新作業，以在 FormView 控制項置於編輯模式。 在指定的內容**EditItemTemplate**屬性會顯示為資料列。 |
| Insert | 「 插入 」 | 用於插入作業，以嘗試使用由使用者所提供的值的資料來源中插入新的記錄。 引發 ItemInserting 和 ItemInserted 事件。 |
| 新增 | "New" | 用於插入作業，以將 FormView 控制項放在插入模式。 在指定的內容**上**屬性會顯示為資料列。 |
| 頁面 | 「 頁面 」 | 用於分頁作業，以代表執行分頁的頁面巡覽列中按鈕。 指定的分頁作業，請設定**CommandArgument** 下一步 」、 「 Prev"、"First"、 「 最後 」，或要瀏覽頁面的索引按鈕的屬性。 引發 PageIndexChanging 和 PageIndexChanged 事件。 |
| 更新 | 「 更新 」 | 用於更新作業，以嘗試更新資料來源中顯示的記錄，使用者所提供的值。 引發 ItemUpdating 和 ItemUpdated 事件。 |

不同於刪除按鈕 （會刪除顯示的資料錄立即），按一下 [編輯] 或 [新增] 按鈕時，控制項進入編輯 FormView 或分別插入模式。 在編輯模式中，內容包含在**EditItemTemplate**屬性會顯示目前的資料項目。 一般而言，編輯項目樣板會定義成將 編輯 按鈕會取代的更新 和 取消 按鈕。 若要修改使用者欄位的值通常也會顯示適用於欄位的資料類型 （例如文字方塊或核取方塊控制項） 的輸入的控制項。 按一下 更新 按鈕更新資料來源中的記錄，而按一下取消 5d; 按鈕會放棄所有變更。

同樣地，將中所包含的內容**上**控制項處於插入模式時，將會顯示之資料項目的屬性。 插入項目範本通常是在 新增 按鈕會取代 Insert 和 取消 5d; 按鈕，而使用者輸入新記錄的值會顯示空白的輸入的控制項的定義。 按一下 插入 按鈕在資料來源插入記錄，而按一下取消 5d; 按鈕會放棄所有變更。

在 FormView 控制項提供分頁功能，可讓使用者巡覽至其他資料來源中的記錄。 啟用時，頁面巡覽列會顯示在 FormView 控制項包含頁面導覽控制項。 若要啟用分頁， **AllowPaging**屬性**true**。 您可以藉由設定 PagerStyle 中包含的物件屬性和 PagerSettings 屬性自訂頁面巡覽區列。 而不是使用內建的頁面巡覽列 UI，您可以建立自己的 UI 使用**PagerTemplate**屬性。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以設定控制項的不同部分的樣式屬性，來自訂 FormView 控制項的外觀。 下表列出不同的樣式屬性。

| **樣式屬性** | **說明** |
| --- | --- |
| EditRowStyle | 樣式設定為資料列時 FormView 控制項處於編輯模式。 |
| EmptyDataRowStyle | 在 FormView 在控制項中顯示資料來源不包含任何記錄時，空白資料列樣式設定。 |
| FooterStyle | 在 FormView 控制項的頁尾資料列樣式設定。 |
| HeaderStyle | 在 FormView 控制項的標頭資料列樣式設定。 |
| InsertRowStyle | 樣式設定為資料列時 FormView 控制項處於插入模式。 |
| PagerStyle | 在控制項中顯示 FormView 啟用分頁功能時的頁面巡覽列樣式設定。 |
| RowStyle | FormView 控制項處於唯讀模式時的資料列樣式設定。 |

## <a name="events"></a>事件

在 FormView 控制項提供數個事件，您可以程式設計的。 這可讓您在事件發生時執行自訂的常式。 下表列出 FormView 控制項所支援的事件。

| **Event** | **說明** |
| --- | --- |
| ItemCommand | 在 FormView 控制項按鈕時發生。 此事件通常用於在控制項中按下按鈕時執行工作。 |
| ItemCreated | 在 FormView 控制項中建立所有 FormViewRow 物件之後發生。 此事件通常用於顯示之前修改記錄的值。 |
| ItemDeleted | 發生於刪除 按鈕 (具有按鈕其**CommandName**屬性設定為"Delete") 按一下時，但之後 FormView 控制從資料來源中刪除記錄。 此事件通常用來檢查刪除作業的結果。 |
| ItemDeleting | 按一下 [刪除] 按鈕時，但控制項之前 FormView 從資料來源刪除記錄時發生。 此事件通常用來取消刪除作業。 |
| ItemInserted | 發生於插入按鈕 (具有按鈕其**CommandName**屬性設定為 「 插入 」) 按一下時，但之後 FormView 控制項插入資料錄。 此事件通常用於檢查插入作業的結果。 |
| ItemInserting | 按一下 [插入] 按鈕時，但在 FormView 之前控制項才會插入記錄時發生。 此事件通常用來取消插入作業。 |
| ItemUpdated | 發生於更新 按鈕 (具有按鈕其**CommandName**屬性設定為 「 更新 」) 按一下時，但在 FormView 控制項更新資料列。 若要檢查更新作業的結果通常使用這個事件。 |
| ItemUpdating | 按一下 [更新] 按鈕時，但在 FormView 之前控制更新的記錄時發生。 此事件通常用來取消更新作業。 |
| ModeChanged | 在 FormView 控制項變更模式之後，就會發生 （若要編輯、 插入或唯讀模式）。 在 FormView 控制項變更模式時執行的工作通常使用這個事件。 |
| ModeChanging | 在 FormView 控制項變更模式之前，就會發生 （若要編輯、 插入或唯讀模式）。 此事件通常用來取消模式變更。 |
| PageIndexChanged | 發生於按下一個頁面巡覽區按鈕時，但之後 FormView 控制處理的分頁作業。 當您需要執行的工作之後使用者巡覽至不同的記錄控制項中, 時，通常使用這個事件。 |
| PageIndexChanging | 發生於按下一個頁面巡覽區按鈕時，但在 FormView 之前控制項負責的分頁作業。 此事件通常用於取消的分頁作業。 |

## <a name="detailsview"></a>在 DetailsView

DetailsView 控制項用來從資料來源的單一記錄顯示在資料表中，其中每個記錄的欄位會顯示在資料表資料列。 它可以用於搭配 GridView 控制項主版詳細資料案例。 在 DetailsView 控制項支援下列功能：

- 繫結至資料來源控制項，例如 SqlDataSource。
- 內建的插入功能。
- 內建更新和刪除功能。
- 內建的分頁功能。
- 在 DetailsView 物件模型，來動態設定屬性，以程式設計方式存取處理事件，等等。
- 透過佈景主題和樣式的可自訂外觀。

## <a name="row-fields"></a>資料列欄位

在 DetailsView 控制項中的每個資料列會藉由宣告欄位控制項建立。 不同的資料列欄位型別決定控制項中的資料列的行為。 欄位控制項衍生自 DataControlField。 下表列出可用的不同的資料列欄位型別。

| **資料行欄位類型** | **說明** |
| --- | --- |
| BoundField | 以文字中顯示資料來源中欄位的值。 |
| ButtonField | 在 DetailsView 控制項中顯示命令按鈕。 這可讓您顯示具有自訂按鈕控制項，例如新增或移除按鈕的資料列。 |
| CheckBoxField | 在 DetailsView 控制項中顯示核取方塊。 這個資料列的欄位型別通常用於顯示具有布林值的欄位。 |
| CommandField | 顯示內建命令按鈕來執行編輯、 插入或刪除 DetailsView 控制項中的作業。 |
| HyperLinkField | 資料來源中的欄位的值顯示為超連結。 這個資料列的欄位型別可讓您將第二個欄位繫結至超連結的 URL。 |
| ImageField | 在 DetailsView 控制項中顯示的影像。 |
| TemplateField | 顯示使用者定義內容 DetailsView 控制項根據指定的範本中的資料列。 這個資料列的欄位型別可讓您建立自訂的資料列的欄位。 |

根據預設，AutoGenerateRows 屬性設定為**true**，自動產生每個欄位可繫結型別的繫結的資料列欄位物件資料來源中。 有效的可繫結類型為字串、 DateTime、 Decimal、 Guid、 和基本型別的集合。 每個欄位然後顯示資料列中為文字，每個欄位出現在資料來源中的順序。

自動產生資料列提供快速簡便的方式來顯示記錄中的每個欄位。 不過，使用 DetailsView 控制項的進階的功能，您必須明確宣告要包括在 DetailsView 控制項中的資料列欄位。 若要宣告的資料列的欄位，請先設定**AutoGenerateRows**屬性**false**。 接下來，加入 開啟和關閉**&lt;欄位&gt;**開頭和結尾標記的 DetailsView 控制項之間的標記。 最後，列出您想要包含在開頭和結尾之間的資料列欄位**&lt;欄位&gt;**標記。 指定資料列欄位都會加入至欄位集合中列出的順序。 **欄位**集合可讓您以程式設計方式管理 DetailsView 控制項中的資料列欄位。

> [!NOTE]
> 自動產生欄位不會加入至欄位集合的資料列。


## <a name="binding-to-data"></a>繫結至資料

在 DetailsView 控制項可以繫結至資料來源控制項，例如**SqlDataSource**或 AccessDataSource，或實作 System.Collections.IEnumerable 介面，例如 System.Data.DataView，任何資料來源System.Collections.ArrayList 和 System.Collections.Hashtable。

您可以使用下列方法之一來 DetailsView 控制項繫結至適當的資料來源類型：

- 若要繫結至資料來源控制項，設定 DataSourceID 屬性 DetailsView 控制項的資料來源控制項的識別碼值。 在 DetailsView 控制項自動繫結至指定的資料來源控制項。 這是慣用的方法繫結至資料。
- 繫結至資料來源實作**System.Collections.IEnumerable**介面、 DetailsView 控制項的 DataSource 屬性設定為資料來源，並接著呼叫進行資料繫結的方法。

## <a name="security"></a>安全性

此控制項可以用來顯示使用者輸入，其中可能包含惡意用戶端指令碼。 請檢查從用戶端可執行的指令碼、 SQL 陳述式，或其他程式碼傳送之後，顯示應用程式中的任何資訊。 ASP.NET 提供使用者輸入來封鎖指令碼和 HTML 的輸入的要求驗證功能。

## <a name="data-operations"></a>資料作業

在 DetailsView 控制項提供內建功能，允許使用者更新、 刪除、 插入和控制項中的項目頁面上。 在 DetailsView 控制項繫結至資料來源控制項，DetailsView 控制項可以讓您利用資料來源控制項的功能，並提供自動更新、 刪除、 插入和分頁功能。

在 DetailsView 控制項可以提供支援的 update、 delete、 insert 和分頁作業與其他類型的資料來源;不過，您必須提供適當的事件處理常式中的這些作業的實作。

在 DetailsView 控制項可自動將新增**CommandField**包含編輯、 刪除或新增按鈕到[autogenerateeditbutton]、[autogeneratedeletebutton]或AutoGenerateInsertButton屬性來設定資料列欄位**true**分別。 不同於 刪除按鈕 （會刪除選取的記錄立即），按一下 編輯 或 新增 按鈕時，控制項進入編輯 DetailsView 或插入模式中，分別。 在編輯模式中，編輯 按鈕會取代的更新 和 取消 按鈕。 使用者修改欄位的值會顯示適用於欄位的資料類型 （例如文字方塊或核取方塊控制項） 的輸入的控制項。 按一下 更新 按鈕更新資料來源中的記錄，而按一下取消 5d; 按鈕會放棄所有變更。 同樣地，在插入模式下，新增 按鈕會取代 Insert 和 取消 5d; 按鈕，並讓使用者輸入新記錄的值會顯示空白的輸入的控制項。

在 DetailsView 控制項提供分頁功能，可讓使用者巡覽至其他資料來源中的記錄。 啟用時，頁面導覽控制項會顯示在頁面巡覽區列。 若要啟用分頁，將 AllowPaging 屬性設定為**true**。 您可以使用 PagerStyle 和 PagerSettings 屬性自訂頁面巡覽區列。

## <a name="customizing-the-user-interface"></a>自訂使用者介面

您可以設定控制項的不同部分的樣式屬性，來自訂 DetailsView 控制項的外觀。 下表列出不同的樣式屬性。

| **樣式屬性** | **說明** |
| --- | --- |
| AlternatingRowStyle | 在 DetailsView 控制項中的交替資料列樣式設定。 當設定這個屬性時，資料列會顯示交替 RowStyle 設定和**AlternatingRowStyle**設定。 |
| CommandRowStyle | 包含內建的命令按鈕 DetailsView 控制項中的資料列樣式設定。 |
| EditRowStyle | 在 DetailsView 控制項時的資料列的樣式設定編輯模式。 |
| EmptyDataRowStyle | 在 DetailsView 控制項中顯示，資料來源不包含任何記錄時，空白資料列樣式設定。 |
| FooterStyle | 在 DetailsView 控制項的頁尾資料列樣式設定。 |
| HeaderStyle | 在 DetailsView 控制項的標頭資料列樣式設定。 |
| InsertRowStyle | 在 DetailsView 控制項時的資料列的樣式設定插入模式。 |
| PagerStyle | 在 DetailsView 控制項的頁面巡覽區列樣式設定。 |
| RowStyle | 在 DetailsView 控制項中的資料列樣式設定。 當**AlternatingRowStyle**屬性也設定時，資料列會顯示交替**RowStyle**設定和**AlternatingRowStyle**設定。 |
| FieldHeaderStyle | 在 DetailsView 控制項的標頭資料行樣式設定。 |

## <a name="events"></a>事件

在 DetailsView 控制項提供數個事件，您可以程式設計的。 這可讓您在事件發生時執行自訂的常式。 下表列出 DetailsView 控制項所支援的事件。 在 DetailsView 控制項也會繼承這些事件及其基底類別： 資料繫結、 資料繫結、 處置、 Init、 負載、 PreRender，以及轉譯。

| **Event** | **說明** |
| --- | --- |
| ItemCommand | DetailsView 控制項中按下按鈕時，就會發生。 |
| ItemCreated | 在 DetailsView 控制項中建立所有 DetailsViewRow 物件之後發生。 此事件通常用於顯示之前修改記錄的值。 |
| ItemDeleted | 發生於按一下刪除] 按鈕時，但之後 DetailsView 控制項從 [資料來源中刪除記錄。 此事件通常用來檢查刪除作業的結果。 |
| ItemDeleting | 按一下 [刪除] 按鈕時，但之前 DetailsView 控制項中刪除記錄從資料來源時發生。 此事件通常用來取消刪除作業。 |
| ItemInserted | 發生於按一下 [插入] 按鈕時，但之後 DetailsView 控制項才會插入記錄。 此事件通常用於檢查插入作業的結果。 |
| ItemInserting | 按一下 [插入] 按鈕時，但之前 DetailsView 控制項才會插入記錄時發生。 此事件通常用來取消插入作業。 |
| ItemUpdated | 按一下 [更新] 按鈕時，但在 DetailsView 控制項更新資料列之後，就會發生。 若要檢查更新作業的結果通常使用這個事件。 |
| ItemUpdating | 按一下 [更新] 按鈕時，但之前 DetailsView 控制項更新記錄時發生。 此事件通常用來取消更新作業。 |
| ModeChanged | 在 DetailsView 控制項變更模式 （編輯、 插入或唯讀模式） 之後，就會發生。 在 DetailsView 控制項變更模式時執行的工作通常使用這個事件。 |
| ModeChanging | 在 DetailsView 控制項變更 （編輯、 插入或唯讀模式） 的模式之前發生。 此事件通常用來取消模式變更。 |
| PageIndexChanged | 發生於按下一個頁面巡覽區按鈕時，但之後 DetailsView 控制項控點的分頁作業。 當您需要執行的工作之後使用者巡覽至不同的記錄控制項中, 時，通常使用這個事件。 |
| PageIndexChanging | 發生於按下一個頁面巡覽區按鈕時，但之前 DetailsView 控制項負責的分頁作業。 此事件通常用於取消的分頁作業。 |

## <a name="the-menu-control"></a>此功能表控制項

ASP.NET 2.0 中的功能表控制項被設計為全功能的瀏覽系統中。 它可以輕鬆地以階層式資料來源，例如 Treeview 的資料繫結。

功能表控制項結構可定義宣告或動態的而且包含單一根節點和任意數目的子節點。 下列程式碼以宣告方式定義功能表控制項的功能表。

[!code-aspx[Main](data-bound-controls/samples/sample4.aspx)]

在上述範例中，Home.aspx 節點是根節點。 其他所有節點都會都巢狀的各個層級的根節點。

有兩個功能表的功能表控制項可以呈現; 類型功能表靜態和動態功能表。 靜態功能表永遠會顯示的功能表項目所組成。 動態功能表時，才會顯示使用者的滑鼠停留它們的功能表項目所組成。 客戶通常混淆功能表以宣告方式定義的靜態功能表和功能表會在執行階段的資料繫結的動態功能表。 事實上，動態和靜態功能表不相關的母體擴展的方法。 條款*靜態*和*動態*只會參考 [] 功能表是不是以靜態方式顯示根據預設，或只有當使用者採取某些動作時顯示。

**StaticDisplayLevels**屬性用來設定功能表多少層級是靜態和因此顯示的預設值。 在上述範例中，設定**StaticDisplayLevels**屬性設為 2 的值會導致要以靜態方式顯示首頁節點、 [音樂] 節點和 [影片] 節點的功能表。 使用者停留在父節點時，就會以動態方式顯示所有其他節點。

**MaximumDynamicDisplayLevels**屬性可設定數目上限可顯示功能表是動態的層級。 在層級高於指定的值的任何動態功能表**MaximumDynamicDisplayLevels**屬性都會被捨棄。

> [!NOTE]
> 它是幾乎可以肯定您可能會遇到不呈現因為 MaximumDynamicDisplayLevels 屬性會出現功能表的情況。 在這些情況下，請確定屬性充分設為允許客戶功能表顯示。


## <a name="data-binding-the-menu-control"></a>資料繫結功能表控制項

此功能表控制項可以繫結，例如 Treeview 或 XMLDataSource 任何階層式資料來源。 Treeview 是最常用方法進行資料繫結到功能表控制項，因此它會送出 Web.sitemap 檔案提供已知的 API 功能表控制項，其結構描述。 下列清單顯示簡單的 Web.sitemap 檔案。

[!code-xml[Main](data-bound-controls/samples/sample5.xml)]

請注意，只有一個根 siteMapNode 項目，在此情況下，首頁項目。 可以針對每個 siteMapNode 設定數個屬性。 最常使用的屬性如下：

- **url**指定要在使用者按一下功能表項目時所顯示的 URL。 如果這個屬性不存在，則按下時只會回傳節點。
- **標題**指定功能表項目顯示的文字。
- **描述**節點做為文件。 當滑鼠停留在節點上時，也會顯示為工具提示。
- **siteMapFile**允許巢狀網站地圖。 這個屬性必須指向語式正確的 ASP.NET sitemap 檔案。
- **角色**ASP.NET 安全性調整控制可讓節點的外觀。

請注意，雖然這些屬性都是選用項目，功能表的行為可能無法預期的如果未指定。 例如，如果*url*指定屬性但*描述*屬性、 節點將不會顯示並不會有無法瀏覽至指定的 URL。

## <a name="controlling-a-menus-operation"></a>控制功能表作業

有幾個屬性會影響 ASP.NET 功能表控制項; 的操作**方向**屬性， **DisappearAfter**屬性， **StaticItemFormatString**屬性，而**StaticPopoutImageUrl**屬性都是幾種。

- **方向**可以設定為 *水平*或*垂直*及控制靜態功能表項目會配置以水平方式，在資料列或以垂直方式和時堆疊另一個。 這個屬性不會影響動態功能表。
- **DisappearAfter**屬性可讓您設定動態功能表應該保留多久可見滑鼠之後，從它的向外移動。 指定的值是以毫秒為單位，預設值為 500。 將此屬性設定為-1 值會讓功能表永遠不會自動消失。 在此情況下，當使用者按一下功能表外，就會只消失功能表。
- **StaticItemFormatString**屬性可便於維護一致的用語功能表系統中。 當指定這個屬性， *{0}*應該出現在資料來源的描述取代輸入。 比方說，才能從練習 1 說瀏覽我們的產品頁面的功能表項目等，您會指定造訪我們 {0} StaticItemFormatString 的頁面。 在執行階段，ASP.NET 會取代出現 {0} 的任何功能表項目的正確的描述。
- **StaticPopoutImageUrl**屬性會指定用來表示特定功能表節點有子節點可存取上方的影像。 動態功能表將會繼續使用預設映像。

## <a name="templated-menu-controls"></a>樣板化的功能表控制項

功能表控制項是一個樣板化控制項，並允許兩個不同項目範本。StaticItemTemplate 和 DynamicItemTemplate。 使用這些範本，您可以輕鬆地將伺服器控制項或使用者控制項加入您的功能表。

若要編輯的範本在 Visual Studio.NET 中，按一下 智慧標籤按鈕功能表上的，選擇編輯樣板。 然後您可以選擇編輯 StaticItemTemplate 或 DynamicItemTemplate 之間。

載入頁面時，會出現靜態功能表中已加入至 StaticItemTemplate 任何控制項。 任何控制項加入至 DynamicItemTemplate 會出現在所有的快顯功能表。

## <a name="menu-events"></a>功能表事件

此功能表控制項有兩個事件都是唯一的。**MenuItemClicked**和**MenuItemDatabound**事件。

按一下功能表項目時，會引發 MenuItemClicked 事件。 資料繫結功能表項目時，會引發 MenuItemDatabound 事件。 **MenuEventArgs**傳遞至事件處理常式提供了透過此項目屬性之功能表項目的存取。

## <a name="controlling-a-menus-appearance"></a>控制功能表外觀

您也可以影響使用一或多個格式功能表可用的許多樣式功能表控制項的外觀。 這些包括**StaticMenuStyle**， **DynamicMenuStyle**， **DynamicMenuItemStyle**，**想像**，和**DynamicHoverStyle**。 這些屬性會設定使用標準 HTML 樣式字串。 例如，下列會影響動態功能表的樣式。

[!code-aspx[Main](data-bound-controls/samples/sample6.aspx)]

> [!NOTE]
> 如果您使用的滑鼠停留樣式，您必須新增&lt;head&gt;與頁面中的項目*runat*元素設定為*伺服器*。


功能表控制項也支援使用 ASP.NET 2.0 佈景主題。

## <a name="the-treeview-control"></a>TreeView 控制項

TreeView 控制項中的樹狀結構顯示資料。 如同功能表控制項，它可以輕鬆地繫結至任何階層式資料來源，例如 Treeview 的資料。

客戶有可能詢問關於 TreeView 控制項在 ASP.NET 2.0 中的第一個問題是不論是否相關以供 ASP.NET TreeView IE WebControl 1.x。 不存在。 ASP.NET 2.0 TreeView 控制項，從頭撰寫，並透過先前可用 IE TreeView WebControl 提供顯著的改進。

我將不會進入如何 TreeView 控制項繫結至網站導覽中完全相同的方式為功能表控制項時執行的詳細資料。 不過，TreeView 控制項的一些不同的差異，其運作方式的方式。

根據預設，出現完全展開樹狀檢視控制項。 若要變更在初始載入時展開的等級，請修改**ExpandDepth**控制項的屬性。 這是特別重要，尤其的情況下，樹狀檢視時展開特定節點的資料繫結。

## <a name="databinding-the-treeview-control"></a>資料繫結 TreeView 控制項

不同於功能表控制項，樹狀檢視本身也是用來處理大量的資料。 因此，除了資料繫結 Treeview 或 XMLDataSource，樹狀檢視通常是資料繫結至資料集或其他關聯式資料。 在其中 TreeView 控制項繫結至大量資料的情況下，最好只繫結至會實際顯示在控制項中的資料。 接著，您可以為樹狀檢視節點展開資料繫結至其他資料。

在這些情況下， **PopulateOnDemand**的樹狀檢視的屬性應該設定為*true*。 然後您必須提供實作**TreeNodePopulate**方法。

## <a name="data-binding-without-postback"></a>資料繫結，而不回傳

請注意，當您第一次展開的節點，在上述範例中，網頁回傳，並重新整理。 Thats 不在此範例中，但是問題可以想像它可能會在實際執行環境具有大量的資料。 更好的案例就是樹狀檢視會仍動態填入其節點，但是會在 post 不回傳至伺服器。

藉由設定**PopulateNodesFromClient**和**PopulateOnDemand**為 true 時，ASP.NET TreeView 控制項的屬性會動態地填入節點，而回傳。 父節點展開時，從用戶端進行 XMLHttp 要求和 OnTreeNodePopulate 事件引發。 伺服器回應與 XML 資料島然後用來資料繫結的子節點。

ASP.NET 會動態建立實作這項功能的用戶端程式碼。 &lt;指令碼&gt;標記包含指令碼會產生指向 AXD-ISAPID-2.0-64 檔案。 例如下, 面清單顯示的指令碼，會產生 XMLHttp 要求的指令碼連結。

[!code-html[Main](data-bound-controls/samples/sample7.html)]

如果您在您的瀏覽器中瀏覽 AXD-ISAPID-2.0-64 檔案上方，並將它開啟，您會看到實作 XMLHttp 要求的程式碼。 這個方法會防止客戶修改指令碼檔案。

## <a name="controlling-the-operation-of-the-treeview-control"></a>TreeView 控制項的作業

TreeView 控制項有數個會影響操作控制項的屬性。 最明顯的屬性是**ShowCheckBoxes**， **ShowExpandCollapse**，和**ShowLines**。

**ShowCheckBoxes**屬性會影響是否節點會顯示核取方塊呈現時。 這個屬性的有效值為**無**，**根**，**父**，**分葉**，和**所有**。 這些會影響樹狀檢視控制項，如下所示：

| **屬性值** | **效果** |
| --- | --- |
| 無 | 核取方塊不會顯示在任何節點上。 這是預設設定。 |
| 根 | 核取方塊才會顯示在根節點上。 |
| 父代 | 核取方塊才會顯示具有子節點的節點上。 這些子節點可以是父節點或分葉節點。 |
| 分葉 | 只有在不具有任何子節點的節點上會顯示核取方塊。 |
| 全部 | 核取方塊會顯示所有節點上。 |

當使用核取方塊時， **CheckedNodes**屬性會傳回在回傳時簽入的樹狀檢視節點的集合。

**ShowExpandCollapse**屬性控制根與父節點旁邊的展開/摺疊影像的外觀。 如果這個屬性設定為**false**，TreeView 節點轉譯為超連結，並會依序按一下連結的 展開/摺疊。

**ShowLines**屬性控制是否要顯示線條連接至子節點的父節點。 當**false** （預設值），會顯示任何線條。 當**true**，TreeView 控制項將使用所指定的資料夾中的線條影像**LineImagesFolder**屬性。

若要自訂 TreeView 線條的外觀，Visual Studio.NET 2005年包含行設計師工具。 您可以存取這項工具使用樹狀檢視控制項做為下面上智慧標籤 按鈕。


![](data-bound-controls/_static/image1.jpg)

**圖 1**


當選取**自訂線條影像**功能表選項，將啟動列設計工具 工具，可讓您設定 TreeView 線條的外觀。

## <a name="treeview-events"></a>樹狀檢視事件

TreeView 控制項具有下列的唯一事件：

- 選取節點時，就會發生 SelectedNodeChanged 根據**SelectAction**屬性。
- 變更節點 checkboxs 狀態時，就會發生 TreeNodeCheckChanged。
- 展開節點時，就會發生 TreeNodeExpanded 根據**SelectAction**屬性。
- TreeNodeCollapsed 發生於摺疊的節點。
- 節點的資料時，就會發生 TreeNodeDataBound 繫結。
- 填入節點時，就會發生 TreeNodePopulate。

**SelectAction**屬性可讓您設定 選取節點時引發的事件。 SelectAction 屬性會提供下列動作：

- TreeNodeSelectAction.Expand 引發 TreeNodeExpanded 選取節點時。
- TreeNodeSelectAction.None 會引發任何事件時選取的節點。
- TreeNodeSelectAction.Select 後，引發 SelectedNodeChanged 事件選取節點。
- SelectedNodeChanged 事件和 TreeNodeExpanded 事件選取節點時，會引發 TreeNodeSelectAction.SelectExpand。

## <a name="controlling-appearance-with-styles"></a>控制使用樣式的外觀

TreeView 控制項提供許多屬性控制與樣式控制項的外觀。 使用下列屬性。

| **屬性名稱** | **控制項** |
| --- | --- |
| HoverNodeStyle | 當滑鼠停留在其上控制節點的樣式。 |
| 示 | 控制的分葉節點的樣式。 |
| 有 | 控制的所有節點的樣式。 特定節點 （例如示） 的樣式會覆寫此樣式。 |
| 如上 | 控制所有父節點的樣式。 |
| 文字左邊 | 控制根節點的樣式。 |
| SelectedNodeStyle | 控制選取之節點的樣式。 |

每一個屬性是唯讀狀態。 不過，他們將每個傳回**這些影像會呈現**物件，該物件的內容可以修改和使用*屬性子屬性*格式。 例如，若要設定**ForeColor**屬性**SelectedNodeStyle**，您可以使用下列語法：

[!code-aspx[Main](data-bound-controls/samples/sample8.aspx)]

請注意，上述的標記不封閉的。 這是因為使用如下所示的宣告式語法時，您會加入樹狀節點中的 HTML 程式碼。

您也可以在程式碼中使用指定的樣式屬性*property.subproperty*格式。 例如，若要設定**ForeColor**屬性**文字左邊**在程式碼中，您會使用下列語法：

[!code-csharp[Main](data-bound-controls/samples/sample9.cs)]

> [!NOTE]
> 針對不同的樣式屬性的完整清單，請參閱這些影像會呈現物件上的 MSDN 文件。


## <a name="the-sitemappath-control"></a>SiteMapPath 控制項

SiteMapPath 控制項 ASP.NET 開發人員提供的階層連結導覽控制項。 如同其他導覽控制項，它可以輕鬆地繫結至資料階層式資料來源，例如 Treeview 或 XmlDataSource。

SiteMapPath 控制項是由 SiteMapNodeItem 物件所組成。 有三種類型的節點。根節點、 父節點和目前的節點。 根節點是階層式結構頂端的節點。 目前的節點表示目前的頁面。 所有其他節點會是父節點。

## <a name="controlling-the-operation-of-the-sitemappath-control"></a>SiteMapPath 控制項的作業

控制作業 SiteMapPath 控制項的屬性如下所示：

| **Property** | **屬性的描述** |
| --- | --- |
| ParentLevelsDisplayed | 控制要顯示多少個父節點。 預設值為-1，不會限制顯示的父節點的數目。 |
| PathDirection | 控制 SiteMapPath 的方向。 有效值為 RootToCurrent （預設值） 和 CurrentToRoot。 |
| PathSeparator | 控制區隔 SiteMapPath 控制項中的節點的字元字串。 預設值是:。 |
| RenderCurrentNodeAsLink | 布林值，控制將目前的節點呈現為連結。 預設值為 False。 |
| SkipLinkText | 協助工具螢幕助讀程式檢視頁面時的協助。 這個屬性可讓螢幕助讀員略過 SiteMapPath 控制項。 若要停用這項功能，將屬性設定為 String.Empty。 |

## <a name="templated-sitemappath-controls"></a>樣板化 SiteMapPath 控制項

SiteMapControl 是樣板化控制項，而且在這種情況，您可以定義不同的範本，用於顯示控制項。 若要編輯 SiteMapPath 控制項中的範本，請按一下智慧標籤上的按鈕控制項並從功能表中選擇 編輯範本。 這會顯示 SiteMapTasks 功能表上，您可以在其中選擇不同的範本，如下所示。


![](data-bound-controls/_static/image2.jpg)

**圖 2**


**NodeTemplate**樣板參照到 SiteMapPath 中的任何節點。 如果節點是根節點或目前節點和**RootNodeTemplate**或**CurrentNodeTemplate**是設定，NodeTemplate 會覆寫。

## <a name="sitemappath-events"></a>SiteMapPath 事件

SiteMapPath 控制項有兩個不衍生自 Control 類別; 事件**ItemCreated**事件和**ItemDataBound**事件。 SiteMapPath 項目建立時，會引發 ItemCreated 事件。 SiteMapPath 節點的資料繫結期間會呼叫 DataBind 方法時，會引發 ItemDataBound。 A **SiteMapNodeItemEventArgs**物件提供存取特定的 SiteMapNodeItem 透過此項目屬性。

## <a name="controlling-appearance-with-styles"></a>控制使用樣式的外觀

下列樣式可供 SiteMapPath 控制項的格式。

| **屬性名稱** | **控制項** |
| --- | --- |
| CurrentNodeStyle | 控制目前節點的文字的樣式。 |
| 文字左邊 | 控制文字的根節點的樣式。 |
| 有 | 控制假設 CurrentNodeStyle 或文字左邊不適用的所有節點文字的樣式。 |

有屬性會覆寫 CurrentNodeStyle 或文字的左邊。 每一個屬性是唯讀的並傳回**樣式**物件。 若要影響使用這些屬性的其中一個節點的外觀，您必須設定傳回樣式物件的屬性。 例如，下列程式碼變更的目前節點的前景色彩屬性。

[!code-aspx[Main](data-bound-controls/samples/sample10.aspx)]

屬性也可以套用以程式設計方式，如下所示：

[!code-csharp[Main](data-bound-controls/samples/sample11.cs)]

> [!NOTE]
> 如果套用範本，將不會套用樣式。


## <a name="lab-1-configuring-an-aspnet-menu-control"></a>實驗室 1： 設定 ASP.NET 功能表控制項

1. 建立新的網站。
2. 選取 檔案 | 新增 | 檔案，然後從檔案範本清單中選擇網站地圖加入站台對應檔案。
3. 開啟站台對應 (根據預設 Web.sitemap)，並修改它，讓它看起來像下列的清單。 實際上不存在連結的站台對應檔案中的頁面，但是，不會針對此練習的問題。

    [!code-xml[Main](data-bound-controls/samples/sample12.xml)]
4. 在設計檢視中開啟預設的 Web 表單。
5. 從 [工具箱] 的 [瀏覽] 區段中，將新的功能表控制項加入網頁。
6. 從 [工具箱] 的 [資料] 區段中，加入新的 Treeview。 在您的網站，Treeview 會自動使用 Web.sitemap 檔案。 (Web.sitemap 檔*必須*站台的根資料夾中。)
7. 按一下功能表控制項，然後按一下 智慧標記按鈕以顯示功能表工作對話方塊。
8. 在 [選擇資料來源] 下拉式清單中，選取 SiteMapDataSource1。
9. 按一下 [自動格式設定] 連結，然後選擇格式功能表。
10. 在 [屬性] 窗格中，設定**StaticDisplayLevels** 2 的屬性。 此功能表控制項現在應該在設計工具中會顯示首頁、 產品和服務 節點。
11. 瀏覽您的瀏覽器使用的功能表中的頁面。 （因為您已新增至站台對應的頁面實際上並不存在，您會看到錯誤時再試一次，並瀏覽至它們。）

試驗變更 StaticDisplayLevels MaximumDynamicDisplayLevels 屬性，請參閱它們如何影響功能表的呈現方式。

## <a name="lab-2-dynamically-binding-a-treeview-control"></a>實驗室 2： 動態地繫結 TreeView 控制項

這個練習會假設您已在本機執行的 SQL Server 和 Northwind 資料庫位於 SQL Server 執行個體。 如果不符合這些條件，請變更此範例中的連接字串。 請注意，您可能也需要指定 SQL Server 驗證，而不是信任連接。

1. 建立新的網站。
2. 切換到 Default.aspx 的程式碼檢視，並以下列程式碼取代所有程式碼。 

    [!code-aspx[Main](data-bound-controls/samples/sample13.aspx)]
3. 將網頁儲存為 treeview.aspx。
4. 瀏覽網頁。
5. 第一次顯示網頁時，您的瀏覽器中檢視網頁的來源。 請注意，只有可見的節點已傳送至用戶端。
6. 按一下任何節點旁邊的加號。
7. 在頁面上檢視原始檔。 請注意，顯示新的節點現在都會出現。

## <a name="lab-3-details-view-and-editing-data-using-a-gridview-and-detailsview"></a>實驗室 3： 詳細資料檢視和編輯使用 GridView 和 DetailsView 的資料

1. 建立新的網站。
2. 網站中加入新的 web.config。
3. 將連接字串加入至 web.config 檔案，如下所示： 

    [!code-xml[Main](data-bound-controls/samples/sample14.xml)]

    > [!NOTE]
    > 您可能需要變更您的環境為基礎的連接字串。
4. 儲存並關閉 web.config 檔案。
5. 開啟 Default.aspx，然後加入新的 SqlDataSource 控制項。
6. 變更 SqlDataSource 控制項的 ID**產品**。
7. 在**SqlDataSource 工作**功能表上，按一下 **設定資料來源**。
8. 選取**Northwind**連接下拉式清單中，按一下 [下一步]。
9. 選取**產品**從**名稱**下拉式清單中，並檢查**ProductID**， **ProductName**， **UnitPrice**，和**UnitsInStock**核取方塊，如下所示。 

![](data-bound-controls/_static/image3.jpg)

    **Figure 3**
10. 按 [ **下一步**]。
11. 按一下 [ **完成**]。
12. 切換至來源檢視，並檢查產生的程式碼。 請注意**SelectCommand**， **DeleteCommand**， **InsertCommand**，和**UpdateCommand**加入到 SqlDataSource控制項。 也請注意已加入的參數。
13. 切換到設計檢視，並將新的 GridView 控制項加入頁面。
14. 選取**產品**從**選擇資料來源**下拉式清單。
15. 請檢查**啟用分頁**和**啟用選取範圍**如下所示。 

![](data-bound-controls/_static/image4.jpg)

    **Figure 4**
16. 按一下**編輯資料行**連結，並確定**自動產生欄位**已核取。
17. 按一下 [確定]。
18. 選取的 GridView 控制項，按一下  按鈕旁邊**DataKeyNames**屬性 窗格中的屬性。
19. 選取**ProductID**從**可用的資料欄位**清單，然後按一下 **&gt;** 按鈕將其加入。
20. 按一下 [確定]。
21. 將新的 SqlDataSource 控制項加入至頁面。
22. 變更 SqlDataSource 控制項的 ID**詳細資料**。
23. 從 SqlDataSource 工作] 功能表中，選擇 [**設定資料來源**。
24. 選擇**Northwind**從下拉式清單中，按一下**下一步**。
25. 選取**產品**從**名稱**下拉式清單中，並檢查 **\*** 中的核取方塊**資料行**清單方塊中。
26. 按一下**其中** 按鈕。
27. 選取**ProductID**從**資料行**下拉式清單。
28. 選取 **=** 運算子下拉式清單中。
29. 選取**控制項**從**來源**下拉式清單。
30. 選取**GridView1**從**控制項 ID**下拉式清單。
31. 按一下**新增**按鈕即可加入 WHERE 子句。
32. 按一下 [確定]。
33. 按一下**進階**按鈕，然後檢查**產生 INSERT、 UPDATE 和 DELETE 陳述式**核取方塊。
34. 按一下 [確定]。
35. 按一下**下一步**按一下**完成**。
36. 在 DetailsView 控制項加入至網頁。
37. 在**選擇資料來源**下拉式清單中，選擇**詳細資料**。
38. 請檢查**啟用編輯**核取方塊，如下所示。 

![](data-bound-controls/_static/image1.gif)

    **Figure 5**
39. 儲存網頁，並瀏覽 Default.aspx。
40. 按一下**選取**不同的記錄會自動看到 DetailsView 更新旁邊的連結。
41. 按一下**編輯**DetailsView 控制項中的連結。
42. 對記錄進行變更，然後按一下**更新**。
