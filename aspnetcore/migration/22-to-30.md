---
title: 從 ASP.NET Core，移轉到 3.0 2.2 預覽
author: tdykstra
description: 了解如何將 ASP.NET Core 2.2 專案移轉至 ASP.NET Core 3.0。
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: abd31fa8ed27af230f347608f3e60f855a46b908
ms.sourcegitcommit: 28a2874765cefe9eaa068dceb989a978ba2096aa
ms.translationtype: MT
ms.contentlocale: zh-TW
ms.lasthandoff: 06/17/2019
ms.locfileid: "67167114"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="1d6f9-103">從 ASP.NET Core 2.2 到 3.0 移轉</span><span class="sxs-lookup"><span data-stu-id="1d6f9-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="1d6f9-104">藉由[Scott Addie](https://github.com/scottaddie)和[Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="1d6f9-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="1d6f9-105">這篇文章說明如何更新現有的 ASP.NET Core 2.2 專案，以 ASP.NET Core 3.0。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-3.0.md)]

## <a name="update-the-project-file"></a><span data-ttu-id="1d6f9-106">更新專案檔</span><span class="sxs-lookup"><span data-stu-id="1d6f9-106">Update the project file</span></span>

* <span data-ttu-id="1d6f9-107">設定[目標 Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks)到`netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-107">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="1d6f9-108">移除任何`<PackageReference>`要[Microsoft.AspNetCore.All](xref:fundamentals/metapackage)或是[Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app)中繼套件。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-108">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="1d6f9-109">更新`Version`屬性上剩餘`<PackageReference>`項目`Microsoft.AspNetCore.*`目前的預覽版本的封裝 (比方說， `3.0.0-preview5-19227-01`)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-109">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="1d6f9-110">如果沒有任何的 3.0 版的封裝，封裝可能已被取代 3.0。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-110">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="1d6f9-111">許多這些套件屬於`Microsoft.AspNetCore.App`，而且不應該單獨參考。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-111">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="1d6f9-112">不會再產生 3.0 中的封裝的初步清單，請參閱[同時停止生產套件共用的 framework 3.0 中的組件 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-112">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="1d6f9-113">*共用的架構*是組件集 ( *.dll*檔案)，它們是電腦上安裝，並且參考`Microsoft.AspNetCore.App`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-113">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="1d6f9-114">如需詳細資訊，請參閱[共用的架構](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) \(英文\)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-114">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="1d6f9-115">值得注意的幾個元件的組件已移除從`Microsoft.AspNetCore.App`3.0。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-115">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="1d6f9-116">新增`<PackageReference>`如果您使用 Api 從封裝中所列的項目[Microsoft.AspNetCore.App 3.0 所移除的組件 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-116">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="1d6f9-117">移除元件的範例包括：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-117">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="1d6f9-118">出貨的組件清單`Microsoft.AspNetCore.App`尚未完成，而且之前 3.0 RTM 將會變更。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-118">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="1d6f9-119">請考慮下列程式碼：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-119">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="1d6f9-120">`ReadAsAsync`上述的程式碼中呼叫的方法納入`Microsoft.AspNet.WebApi.Client`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-120">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="1d6f9-121">安裝[Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet 套件以解決在 3.0 中的編譯問題。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-121">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="1d6f9-122">新增[Json.NET 支援](#jsonnet-support)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-122">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="1d6f9-123">專案預設值為[同處理序主控模型](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model)在 ASP.NET Core 3.0 或更新版本。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-123">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="1d6f9-124">您可以選擇性移除`<AspNetCoreHostingModel>`屬性中的專案檔，如果其值為`InProcess`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-124">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="1d6f9-125">Json.NET 支援</span><span class="sxs-lookup"><span data-stu-id="1d6f9-125">Json.NET support</span></span>

<span data-ttu-id="1d6f9-126">工作的一部分[改善 ASP.NET Core 的共用的架構](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/)， [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm)已從 ASP.NET Core 的共用架構。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-126">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="1d6f9-127">若要使用 Json.NET 在 ASP.NET Core 3.0 專案：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-127">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="1d6f9-128">新增的套件參考[Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-128">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="1d6f9-129">更新`Startup.ConfigureServices`呼叫`AddNewtonsoftJson`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-129">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="1d6f9-130">Json.NET 設定可在呼叫`AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-130">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="1d6f9-131">MVC 服務登錄</span><span class="sxs-lookup"><span data-stu-id="1d6f9-131">MVC service registration</span></span>

<span data-ttu-id="1d6f9-132">ASP.NET Core 3.0 新增新的選項，以註冊內的 MVC 案例`Startup.ConfigureServices`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-132">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="1d6f9-133">三個新的最上層的擴充方法與在 MVC 案例`IServiceCollection`可用。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-133">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="1d6f9-134">範本會使用這些新的方法，而不是`UseMvc`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-134">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="1d6f9-135">不過，`AddMvc`會繼續如之前的版本中運作。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-135">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="1d6f9-136">下列範例會將控制站和 API 相關的功能，但無法檢視或網頁的支援。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-136">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="1d6f9-137">[API] 範本會使用此程式碼：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-137">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="1d6f9-138">下列範例會將控制站、 API 相關的功能，並檢視中，但不是頁面的支援。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-138">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="1d6f9-139">Web 應用程式 (MVC) 範本會使用此程式碼：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-139">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="1d6f9-140">下列範例會將支援 Razor 頁面與最小的控制器支援。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-140">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="1d6f9-141">Web 應用程式範本會使用此程式碼：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-141">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="1d6f9-142">也可以結合新的方法。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-142">The new methods can also be combined.</span></span> <span data-ttu-id="1d6f9-143">下列範例相當於呼叫`AddMvc`ASP.NET Core 2.2 中：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-143">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="1d6f9-144">更新路由的啟始程式碼</span><span class="sxs-lookup"><span data-stu-id="1d6f9-144">Update routing startup code</span></span>

<span data-ttu-id="1d6f9-145">如果應用程式呼叫`UseMvc`或是`UseSignalR`，移轉應用程式[端點路由](xref:fundamentals/routing)的話。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-145">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="1d6f9-146">若要改善端點路由與舊版 MVC 的相容性，我們已在 ASP.NET Core 2.2 中導入的 URL 產生的一些變更還原。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-146">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="1d6f9-147">如果您遇到使用 2.2 中的 端點路由的問題時，預期在 ASP.NET Core 3.0 改進，但有下列例外狀況：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-147">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="1d6f9-148">如果應用程式實作`IRouter`或繼承自`Route`，您可能想要避免這一次移轉。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-148">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="1d6f9-149">提供在提供意見反應[計劃移轉到端點路由 IRouter 基礎實作](https://github.com/aspnet/AspNetCore/issues/4221)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-149">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="1d6f9-150">如果應用程式直接存取`RouteData.Routers`在 MVC 中，您可能想要避免這一次移轉。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-150">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="1d6f9-151">提供在提供意見反應[移轉指導方針使用 RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-151">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="1d6f9-152">端點路由支援相同的路由模式語法和撰寫功能的路由模式`IRouter`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-152">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="1d6f9-153">端點路由支援`IRouteContraint`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-153">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="1d6f9-154">端點的路由支援`[Route]`， `[HttpGet]`，和其他 MVC 路由屬性。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-154">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="1d6f9-155">大部分的應用程式只`Startup`需要做的變更。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-155">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="1d6f9-156">移轉 Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="1d6f9-156">Migrate Startup.Configure</span></span>

<span data-ttu-id="1d6f9-157">建議這麼做：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-157">General advice:</span></span>

* <span data-ttu-id="1d6f9-158">新增`UseRouting`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-158">Add `UseRouting`.</span></span> 
* <span data-ttu-id="1d6f9-159">如果應用程式呼叫`UseStaticFiles`，將`UseStaticFiles`**之前** `UseRouting`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-159">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="1d6f9-160">如果應用程式使用驗證/授權功能 (例如 `AuthorizePage` 或 `[Authorize]`)，請將對 `UseRouting` 的呼叫放在 `UseAuthentication` 與 `UseAuthorization` 之**後**。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-160">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="1d6f9-161">如果應用程式中使用[CORS](xref:security/cors)功能，例如`[EnableCors]`，將`UseCors`下一步。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-161">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="1d6f9-162">取代`UseMvc`或是`UseSignalR`使用`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-162">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="1d6f9-163">以下是範例`Startup.Configure`典型的 ASP.NET Core 2.2 應用程式中：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-163">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="1d6f9-164">在上一個更新之後`Startup.Configure`程式碼：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-164">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="1d6f9-165">安全性中介軟體的指導方針</span><span class="sxs-lookup"><span data-stu-id="1d6f9-165">Security middleware guidance</span></span>

<span data-ttu-id="1d6f9-166">授權和 CORS 支援統一周圍[中介軟體](xref:fundamentals/middleware/index)方法。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-166">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="1d6f9-167">這是在這些情況下允許使用相同的中介軟體和功能。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-167">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="1d6f9-168">在此版本中，提供更新的授權中介軟體和 CORS 中介軟體已增強，使它可以了解 MVC 控制站所使用的屬性。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-168">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="1d6f9-169">CORS</span><span class="sxs-lookup"><span data-stu-id="1d6f9-169">CORS</span></span>

<span data-ttu-id="1d6f9-170">先前，可能難以設定 CORS。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-170">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="1d6f9-171">中介軟體提供使用，在某些使用案例，但 MVC 篩選條件適用於**而不需要**在其他中的介軟體的使用案例。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-171">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="1d6f9-172">ASP.NET Core 3.0 中，我們建議所有的應用程式需要 CORS，在搭配端點的路由中使用 CORS 中介軟體。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-172">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="1d6f9-173">`UseCors` 隨附預設原則，並`[EnableCors]`和`[DisableCors]`屬性可用來覆寫預設原則，視需要。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-173">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="1d6f9-174">在以下範例中：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-174">In the following example:</span></span>

* <span data-ttu-id="1d6f9-175">所有的端點，以啟用 CORS`default`命名原則。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-175">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="1d6f9-176">`MyController`類別會停用的 CORS`[DisableCors]`屬性。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-176">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="1d6f9-177">Authorization</span><span class="sxs-lookup"><span data-stu-id="1d6f9-177">Authorization</span></span>

<span data-ttu-id="1d6f9-178">在舊版的 ASP.NET Core 中，透過提供授權支援`[Authorize]`屬性。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-178">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="1d6f9-179">授權中介軟體無法使用。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-179">Authorization middleware wasn't available.</span></span> <span data-ttu-id="1d6f9-180">在 ASP.NET Core 3.0 中，授權中介軟體則是必要項目。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-180">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="1d6f9-181">建議您放在 ASP.NET Core 授權中介軟體 (`UseAuthorization`) 之後立即`UseAuthentication`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-181">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="1d6f9-182">授權中介軟體也可以設定預設原則，可以覆寫。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-182">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="1d6f9-183">在 ASP.NET Core 3.0 或更新版本中，`UseAuthorization`中稱為`Startup.Configure`，以及下列`HomeController`中使用者需要帶正負號：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-183">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="1d6f9-184">如果應用程式中使用`AuthorizeFilter`MVC 中的全域篩選器，建議您重構程式碼，以提供一項原則`UseAuthorization`中介軟體。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-184">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="1d6f9-185">在下列範例中，要套用至所有的自訂原則會要求時`UseAuthorization`呼叫，而`HomeController`允許存取，而不需要讓使用者登入應用程式：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-185">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="1d6f9-186">由中介軟體的授權就不會有任何特定的知識，授權的 framework。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-186">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="1d6f9-187">比方說，[健康狀態檢查](xref:host-and-deploy/health-checks)一無所知特定的授權，但健康情況檢查可以有一個中介軟體所套用的可設定的授權原則。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-187">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="1d6f9-188">在下列範例中，`UseAuthorization`處理授權，沒有預設原則，但`/healthz`健康狀態檢查端點會要求使用者在`admin`角色：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-188">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="1d6f9-189">保護被實作在某些情況下。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-189">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="1d6f9-190">`UseEndpoint` 如果因為遺漏的中介軟體而略過授權或 CORS 原則是中介軟體，則會擲回例外狀況。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-190">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="1d6f9-191">分析器支援服務，以提供其他意見反應的相關設定錯誤正在進行中。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-191">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="1d6f9-192">移轉 SignalR</span><span class="sxs-lookup"><span data-stu-id="1d6f9-192">Migrate SignalR</span></span>

<span data-ttu-id="1d6f9-193">現在的 SignalR 中樞對應發生內`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-193">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="1d6f9-194">對應與每個中樞`MapHub`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-194">Map each hub with `MapHub`.</span></span> <span data-ttu-id="1d6f9-195">如同舊版中，明確列出每個中樞。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-195">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="1d6f9-196">在下列範例中，支援`ChatHub`SignalR 中樞新增：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-196">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="1d6f9-197">移轉 MVC 控制器</span><span class="sxs-lookup"><span data-stu-id="1d6f9-197">Migrate MVC controllers</span></span>

<span data-ttu-id="1d6f9-198">現在對應的控制站發生內`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-198">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="1d6f9-199">新增`MapControllers`如果應用程式中使用屬性路由。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-199">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="1d6f9-200">由於路由包含支援許多架構在 ASP.NET Core 3.0 或更新版本，新增屬性路由的控制器是選用功能。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-200">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="1d6f9-201">取代下列項目：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-201">Replace the following:</span></span>

* <span data-ttu-id="1d6f9-202">`MapRoute` 使用 `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="1d6f9-202">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="1d6f9-203">`MapAreaRoute` 使用 `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="1d6f9-203">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="1d6f9-204">由於路由現在包含不只是 MVC 支援，所以術語已變更為讓這些方法應該清楚地陳述它們的功用。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-204">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="1d6f9-205">慣例路由，例如`MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute`就會新增的順序套用。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-205">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="1d6f9-206">先放置更明確的路由 （例如區域的路由）。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-206">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="1d6f9-207">在以下範例中：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-207">In the following example:</span></span>

* <span data-ttu-id="1d6f9-208">`MapControllers` 新增支援屬性路由的控制器。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-208">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="1d6f9-209">`MapAreaControllerRoute` 新增控制器慣例路由的區域。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-209">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="1d6f9-210">`MapControllerRoute` 新增控制器慣例路由。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-210">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="1d6f9-211">Razor 頁面移轉</span><span class="sxs-lookup"><span data-stu-id="1d6f9-211">Migrate Razor Pages</span></span>

<span data-ttu-id="1d6f9-212">現在對應的 Razor 頁面發生內`UseEndpoints`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-212">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="1d6f9-213">新增`MapRazorPages`如果應用程式中使用 Razor 頁面。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-213">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="1d6f9-214">因為端點路由包含支援許多架構中，新增 Razor Pages 現在選用功能。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-214">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="1d6f9-215">在下列範例中，`MapRazorPages`新增支援 Razor 頁面：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-215">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="1d6f9-216">使用 MVC，而不需要端點的路由</span><span class="sxs-lookup"><span data-stu-id="1d6f9-216">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="1d6f9-217">使用透過 MVC`UseMvc`或是`UseMvcWithDefaultRoute`ASP.NET Core 3.0 需要明確選擇單元內`Startup.ConfigureServices`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-217">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="1d6f9-218">這是必要的因為 MVC 必須知道是否它可以依賴的授權和 CORS 中介軟體在初始化期間。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-218">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="1d6f9-219">分析器會提供，會發出警告如果應用程式會嘗試使用不支援的組態。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-219">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="1d6f9-220">如果應用程式需要舊版`IRouter`支援，請停用`EnableEndpointRouting`使用下列任一方法中， `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-220">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="1d6f9-221">移轉健康情況檢查</span><span class="sxs-lookup"><span data-stu-id="1d6f9-221">Migrate health checks</span></span>

<span data-ttu-id="1d6f9-222">健康情況檢查可以用作*路由器軟體*端點路由。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-222">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="1d6f9-223">新增`MapHealthChecks`使用健康狀態檢查端點的路由。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-223">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="1d6f9-224">`MapHealthChecks`方法會接受引數類似`UseHealthChecks`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-224">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="1d6f9-225">使用的優點`MapHealthChecks`透過`UseHealthChecks`是套用授權，以及可以更精細的比對原則控制的能力。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-225">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="1d6f9-226">在下列範例中，`MapHealthChecks`會針對在健康狀態檢查端點呼叫`/healthz`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-226">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="1d6f9-227">HostBuilder 取代 WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="1d6f9-227">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="1d6f9-228">ASP.NET Core 3.0 範本會使用[泛型主機](xref:fundamentals/host/generic-host)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-228">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="1d6f9-229">使用的舊版[Web 主機](xref:fundamentals/host/web-host)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-229">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="1d6f9-230">下列程式碼顯示產生的 ASP.NET Core 3.0 範本`Program`類別：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-230">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="1d6f9-231">下列程式碼會顯示在範本產生的 ASP.NET Core 2.2`Program`類別：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-231">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="1d6f9-232"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> 會保留在 3.0 中，而且類型`webBuilder`上述的程式碼範例所示。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-232"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="1d6f9-233"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> 會在未來版本中已被取代，並取代`HostBuilder`。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-233"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="1d6f9-234">從最重大的改變`WebHostBuilder`要`HostBuilder`處於[相依性插入 (DI)](xref:fundamentals/dependency-injection)。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-234">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="1d6f9-235">使用時`HostBuilder`，您只可以插入<xref:Microsoft.Extensions.Configuration.IConfiguration>並<xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment>成`Startup`的建構函式。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-235">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="1d6f9-236">`HostBuilder` DI 條件約束：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-236">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="1d6f9-237">啟用 DI 容器來建置一次。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-237">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="1d6f9-238">可避免產生的物件存留期問題，例如解決多個單一執行個體。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-238">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="1d6f9-239">更新 SignalR 的程式碼</span><span class="sxs-lookup"><span data-stu-id="1d6f9-239">Update SignalR code</span></span>

<span data-ttu-id="1d6f9-240">`System.Text.Json` 是現在用戶端和伺服器所使用的預設中樞通訊協定。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-240">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="1d6f9-241">在  `Startup.ConfigureServices`，呼叫`AddJsonProtocol`設定序列化程式的選項。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-241">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="1d6f9-242">**伺服器：**</span><span class="sxs-lookup"><span data-stu-id="1d6f9-242">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="1d6f9-243">**用戶端：**</span><span class="sxs-lookup"><span data-stu-id="1d6f9-243">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="1d6f9-244">切換至 Newtonsoft.Json</span><span class="sxs-lookup"><span data-stu-id="1d6f9-244">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="1d6f9-245">如果您使用的功能`Newtonsoft.Json`不支援的`System.Text.Json`，您可以切換回`Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-245">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="1d6f9-246">安裝[Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet 套件。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-246">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="1d6f9-247">在用戶端，鏈結`AddNewtonsoftJsonProtocol`方法呼叫`HubConnectionBuilder`執行個體：</span><span class="sxs-lookup"><span data-stu-id="1d6f9-247">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="1d6f9-248">在伺服器上，鏈結`AddNewtonsoftJsonProtocol`方法呼叫`AddSignalR`方法呼叫中`Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="1d6f9-248">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="1d6f9-249">選擇在執行階段編譯</span><span class="sxs-lookup"><span data-stu-id="1d6f9-249">Opt in to runtime compilation</span></span>

<span data-ttu-id="1d6f9-250">在 3.0 中，執行階段編譯是選擇性的案例。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-250">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="1d6f9-251">若要啟用執行階段編譯，請參閱<xref:mvc/views/view-compilation#runtime-compilation>。</span><span class="sxs-lookup"><span data-stu-id="1d6f9-251">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
